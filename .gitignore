# Build artifacts
target/
*.o
*.a
*.so
*.dll
*.exe

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# gRPC / generated
*.proto.*

# Debug
*.dSYM/
__pycache__/

# GHA CI artifacts
dist/*.tar.gz

# MIT Submission Package (not needed in code repo)
../MIT-Submission-Package/
../A_Research_Papers/
../B_Four_Model_Consensus/
../C_Language_Specification/
../D_Executive_and_Presentation/
../F_Project_Management/
../MIT-Submission-Package/
../GARNET_*.md
../_*.md

# Redundant files
../WORKSPACE_README.md

# Installer build outputs
../installer/*.tar.gz

# Local agent knowledge (not part of the language)
.garnet-cache/
.codex/
garnet-cli/.garnet-cache/
# Per-slice scratch plans — the ~104 already committed stay for history, but stop
# tracking NEW ones (S126 cleanup; they are working notes, not project truth).
.agent/plans/

# Paper VI Exp 3 harness run output (ephemeral; the harness ships shape, not results)
benchmarks/paper_vi_exp3_compiler_as_agent/out/

# Old dist artifacts (we ship via releases, not repo)
dist/

# target from builds
target/

# Local dogfood evidence bundles — durable copies live in ~/Desktop/dogfood
# (per CLAUDE.md); the in-repo dir is local-only working evidence, never committed.
dogfood/
