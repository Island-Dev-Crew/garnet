# v06: marks the hook as no-capability but leaves intent vague.
@caps()
def before_compile() {
}

def combine(a, b, c, d) {
  let total = a + b + c + d
  total
}

@caps()
def main() {
  before_compile()
  combine(1, 2, 3, 4)
}
