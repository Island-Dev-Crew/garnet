# Agent Coordination Ledger — v0.7 Stream

**Single source of truth for the four-agent v0.7 build-out.**

This file is read by every agent at the start of every session, before any tool
call that writes. If you are an agent reading this: identify your slot, find your
slice, read your PRD, then append your STARTED entry under your slot's section
in the Status Board.

If you are Jon: this file is your dashboard. Each agent appends; you read.

---

## Mission

Close slices S15–S19 to land Garnet v0.7.0. Five slices, four agents working in
parallel. The split is engineered so each agent's writable crates do not overlap;
where slices depend on each other, the trait-first pattern unblocks the dependent
slice with a stub.

---

## Slot Assignments

| Slot | Machine | Harness | Slice(s) | Owned crates (writable) | Read-only |
|---|---|---|---|---|---|
| **mac-opus** | Mac | Claude Code Opus 4.7 1M Max | **S15** (CST — rowan crate, built cold) | `garnet-cst` (new), `garnet-parser-v0.3` (lexer/API dependency; **do not touch `src/cst.rs` #221**) | everything else |
| **win-codex** | Windows | Codex Desktop GPT-5.5 Pro Extra High Fast | **S16** (LSP precision — unblocked after S15-Compare) | `garnet-lsp`, `editors/vscode` | `garnet-cst`, `garnet-parser-v0.3`, `garnet-check-v0.3` |
| **win-opus** | Windows | Claude Code Opus 4.7 1M Max | **S17** (stdlib + layer policy + `@stability`) | `garnet-stdlib`, `garnet-check-v0.3` (stability surface only) | parser, interp, vm, lsp, cst |
| **mac-codex** | Mac | Codex Desktop GPT-5.5 Pro Extra High Fast | **S18** (packages) + **S19** (LLM tier) | `garnet-suggest-llm` (new), `garnet-lang/*` (external repos) | `garnet-check-v0.3`, `garnet-stdlib` |

---

> **v0.7 build-both-then-compare (#221).** A Codex PR (#221) already merged a
> hand-rolled in-parser CST (`garnet-parser-v0.3/src/cst.rs`) + ~578 lines of
> LSP. v0.7 does NOT override it: mac-opus builds a rowan `garnet-cst` crate
> independently and additively (preserve #221's `src/cst.rs` untouched), then a
> **S15-Compare** checkpoint (Jon) picks the canonical CST. S15-Compare on
> 2026-05-24 chose the rowan `garnet-cst` crate as canonical; #221 remains a
> temporary legacy migration oracle until rowan-backed LSP migration is green.
> See `GARNET_v0_7_SLICE_DOGFOOD.md` → S15-Compare.

## Dependency Graph

```
#221 in-parser CST (already merged) ──┐
                                       ├──► S15-Compare (Jon) ──► canonical CST
S15 (mac-opus): rowan garnet-cst ──────┘            │
  ├─→ PR-1: CST trait stub                          │
  └─→ PR-2: full rowan impl (built cold)            ▼
                                              S16 (win-codex)
                                              unblocked after S15-Compare;
                                              targets rowan garnet-cst

S17 (win-opus) ───────────────────────────────────────────┐
                                                          ▼
                                                     S18 (mac-codex)
                                                     starts after S17 MERGED

S17 (win-opus, soft) ─────────────────────────────────────┐
                                                          ▼
                                                     S19 (mac-codex)
                                                     can run in parallel with S18
                                                     once S17 ships @stability
```

**Critical sync points**:

1. **mac-opus builds the rowan `garnet-cst` crate cold** (independently of #221's
   in-parser CST), additively, preserving `garnet-parser-v0.3/src/cst.rs`. S17
   (win-opus) has no CST dependency and can start immediately in parallel.
2. **S15-Compare (Jon) picked the canonical CST** once mac-opus's S15 became
   dogfood-passing. Decision: rowan `garnet-cst` is canonical; #221 remains a
   temporary migration oracle.
3. **win-codex's S16 is unblocked** and targets rowan `garnet-cst`. Do not
   delete #221's parser CST until rowan-backed LSP migration is green.
4. **win-opus's S17 must MERGE before mac-codex starts S18 substantive work.**

---

## Cross-Cutting Files (Multi-Agent Coordination Required)

These files are touched by multiple agents. Use the edit pattern listed.

| File | Edit pattern | Lock holder |
|---|---|---|
| `CHANGELOG.md` | Append your slice as a separate bullet under `[Unreleased]`. Append-only. | none |
| `Cargo.toml` (workspace root) | Add your new crate name to alphabetically-sorted `members = [...]`. Append-sort. | none |
| `CURRENT_STATE.md` | Update only the section under your slice. Section-scoped. | none |
| `F_Project_Management/GARNET_v0_7_SLICE_DOGFOOD.md` | Update only your slice's contract block. Section-scoped. | none |
| `scripts/garnet_mit_readiness_status.py` | Add only your slice's lane definition. Section-scoped. | none |
| `README.md` | **DO NOT EDIT** without a Handoff Request approved by Jon. | Jon |
| `C_Language_Specification/GARNET_v1_0_Mini_Spec.md` | **DO NOT EDIT** without a Handoff Request approved by Jon. | Jon |

If you need to edit a cross-cutting file in a way the pattern doesn't permit,
file a Handoff Request in the section at the bottom of this ledger.

---

## Branch Naming

```
agent-<slot>/<slice>-<short>
```

Examples:
- `agent-mac-opus/s15-cst-trait-stub`   ← S15 PR-1 (small, first)
- `agent-mac-opus/s15-cst-rowan`        ← S15 PR-2 (substantive)
- `agent-win-codex/s16-lsp-rename`
- `agent-win-opus/s17-stability-annot`
- `agent-mac-codex/s18-llm-package`
- `agent-mac-codex/s19-suggest-llm`

PR title format: `S<N>: <short description>`.

---

## Status Board

**Each agent appends entries under their own slot section. Do NOT edit another
agent's section. Other agents read this section every session — keep your entries
accurate and current.**

### mac-opus (S15)
- [2026-05-22 20:25 CDT] STARTED agent-mac-opus/s15-cst-trait-stub — claiming S15. Building the rowan `garnet-cst` crate cold + additive per PRD A; parser `src/cst.rs` (#221) left untouched as the S15-Compare baseline. Baseline verified at main tip `c1fc957`: `cargo test --workspace` = 96 suites ok / 0 failed (exit 0); readiness 78.0%; `rowan` not yet in `Cargo.lock` (will add, must clear `cargo deny`). Confirmed `garnet_parser::lex_source*` is already `pub` (trivia-preserving), so `garnet-cst` reuses the lexer as a dependency — no parser-internal change required for PR-1/PR-2. Plan at `.agent/plans/mac-opus-S15-plan.md`; awaiting Jon approval before opening PR-1.
- [2026-05-22 20:59 CDT] PR-OPEN PR#225 (`agent-mac-opus/s15-cst-trait-stub`) — S15 PR-1: garnet-cst trait surface + stub. New rowan crate (SyntaxKind/GarnetLanguage/CstNode/Parse/parse_cst stub), 6 tests + doc-test + proptest. Local gates all green (fmt/clippy/workspace-test/build-release/`deny`/readiness-no-regression/agent-contracts); CI: `PR dogfood evidence` + `agent documentation contracts` pass, matrix/clippy/deny running. #221 `src/cst.rs` untouched (verified). Desktop bundle `garnet-s15-cst-trait-stub-20260523T015601Z` sealed. Paused before PR-2 per Jon. **Note:** 2 pre-existing docs-sync unittest failures on origin/main (`test_garnet_adoption_surface_status`, `test_garnet_converter_status`, from #221) — not S15, not in CI.
- [2026-05-22 21:18 CDT] REVIEW PR#225 — CI fully green (`gh pr checks` EXIT=0: 27 pass / 2 skip / 0 fail / 0 pending); `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, not draft; PR-body dogfood gate passes; no review findings yet. Ready for PR-Agent Grep Loop + Jon's final merge call. PR-2 (substantive rowan builder) held until #225 merges, per Jon.
- [2026-05-23 03:22 UTC] MERGED PR#225 (squash `4110e1a` on origin/main) — S15 PR-1 trait surface + stub landed; `garnet-cst` crate present on main. #221 `src/cst.rs` baseline preserved.
- [2026-05-22 23:04 CDT] STARTED agent-mac-opus/s15-cst-rowan — S15 PR-2 (substantive). Branched from fresh `origin/main` (`4110e1a`). Built the rowan CST builder cold (direct recursive-descent over the token stream → real composite structure), typed nodes, and `cst_to_ast`. Validated: examples corpus + arbitrary-UTF-8 `proptest` round-trip 100% byte-identical; `cst_to_ast` span-normalized structural parity vs `parse_source` across the corpus; `parse_cst_vs_ast` bench ≈0.99× the AST path (under the 1.5× gate). Added `parser_cst_migration` readiness lane (78.0%→78.8%), regenerated baseline. #221 `src/cst.rs` still untouched. Preparing dogfood bundle + PR-2.
- [2026-05-22 23:12 CDT] PR-OPEN PR#226 (`agent-mac-opus/s15-cst-rowan`) — S15 PR-2: trivia-preserving CST via rowan (builder + nodes + `cst_to_ast` + bench). All 9 dogfood-block/gate commands green locally (fmt/build-release/`-p garnet-cst -p garnet-parser`/bench/workspace-test/clippy/`deny`/readiness-no-regression/agent-contracts, all EXIT 0). Roundtrip 100% (corpus + proptest); `cst_to_ast` span-normalized parity vs `parse_source` on the corpus; bench ≈0.99× AST. New `parser_cst_migration` lane (78.0%→78.8%); baseline regenerated. PR-body dogfood gate passes. Desktop bundle `garnet-s15-cst-rowan-20260523T040956Z` sealed. #221 `src/cst.rs` untouched (verified). Awaiting Jon review/merge; canonical-CST choice = S15-Compare.
- [2026-05-22 23:18 CDT] REVIEW PR#226 — CI fully green (`gh pr checks` EXIT=0: 27 pass / 2 skip / 0 fail / 0 pending); `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, not draft; PR-body dogfood gate passes; no review findings yet. Ready for PR-Agent Grep Loop + Jon's final merge call. **S15 status:** PR-1 (#225) merged + PR-2 (#226) review-ready ⇒ the rowan `garnet-cst` is dogfood-passing. Next gate is **S15-Compare** (Jon, fresh eyes) to pick the canonical CST before S16 unblocks. I do not merge.
- [2026-05-24 15:15 CDT] MERGED PR#226 (squash `3e45625` on origin/main) — S15 PR-2 landed. S15-Compare recorded in `F_Project_Management/DOGFOOD/S15_CST_COMPARE.md`: rowan `garnet-cst` is canonical for v0.7/S16; #221 parser CST remains a temporary legacy oracle until rowan-backed LSP migration is green. Reconciliation preserved #221's useful token/span surface in `garnet-cst/src/tokens.rs` and added parser-CST token parity tests.

### win-codex (S16)
- [2026-06-03 10:05 CDT] STARTED agent-win-codex/s106-windows-cross-os-proof-phase1 — fresh branch from `origin/main` `f18da44` (S105 merged; prompt's `319e883` Stage V closeout confirmed five commits back). Scope is Phase 1 of the Windows proof lane: re-run the Stage V `@max_depth` and `@caps` VM/interp traps on Windows, record WSL as execution/portability only, and stop after PR/merge without starting domain or ultrapunch reproduction. Plan at `.agent/plans/win-codex-S106-phase1-plan.md`.
- [2026-06-03 10:45 CDT] PR-OPEN PR#333 (`Navigata1:agent-win-codex/s106-windows-cross-os-proof-phase1` -> `Island-Dev-Crew:main`, fork PR). S106 Phase 1 local proof is green: S106 proof tests 4/4, S106 proof gate `ok: true` with Windows `enforcement-proof` and WSL `execution-portability`, readiness tests 24/24, readiness no-regression **90.4%** with `windows_cross_os_enforcement_phase1`, `python3 scripts/garnet_mit_readiness_status.py` exit 0, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo fmt --all -- --check` exit 0, and `git diff --check` exit 0. Evidence bundles are committed under `proofs/windows/enforcement/` and `proofs/linux/execution/`, each with `MANIFEST.sha256`. Honest scope: Windows re-proves Stage V traps; WSL is portability only, not seccomp/OS-sandbox enforcement; S103 ultrapunch and S105 domains remain Phase 2.
- [2026-06-03 10:54 CDT] REVIEW PR#333 — Grep Loop reached **5/5** on the S106 Phase 1 scope. Checks covered changed-file inventory (26 files), PR-body dogfood headings (local `check_dogfood_pr_body.py` pass; remote `PR dogfood evidence` pass), overclaim scan for production/1.0, WSL enforcement, seccomp, OS-sandbox, S103, and S105 wording (only intended negative/non-claim boundary lines found), S106 gate `ok: true`, committed proof manifests, readiness no-regression **90.4%**, and remote CI on head `fa845a7` (CodeQL, dogfood, determinism, Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, package smokes, security all pass; release/publish skipped as expected). Ready for authenticated squash merge after this review ledger record gets a final green head.
- [2026-06-03 08:52 CDT] MERGED PR#333 (squash `357bb28` on `origin/main`) — S106 Phase 1 landed via the Chrome Work profile after 26 successful checks and 2 expected skips. Scope remains Windows Stage V trap re-proof plus WSL execution/portability only; no S103/S105 Phase 2, seccomp, Wasmtime fuel, or OS-sandbox enforcement claim.
- [2026-06-03 08:52 CDT] STARTED agent-win-codex/s111-windows-wsl-domain-repro — fresh branch from `origin/main` `357bb28` after S106 merged. Baseline before edits: readiness **90.4%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope: make the Studio/domain proof matrix commit-friendly, record Windows and WSL domain execution evidence as WSL portability (not enforcement), update `.dogfood/goal.json` for S106's merge, and preserve Linux desktop/Tauri GUI launch as still pending without overclaiming.
- [2026-06-03 09:06 CDT] PR-OPEN PR#334 (`Navigata1:agent-win-codex/s111-windows-wsl-domain-repro` -> `Island-Dev-Crew:main`, fork PR). S111 local proof is green: committed reporter lookup TDD red->green, readiness tests 25/25, readiness no-regression **90.6%** with `windows_linux_domain_proof_matrix` now committed/verified, Windows domain matrix 20/20 cases and 60/60 commands under `proofs/windows/domains/windows-domain-matrix-20260603-0855/`, WSL domain matrix 20/20 cases and 60/60 commands under `proofs/linux/execution/domains/wsl-domain-matrix-20260603-0857/`, both manifests verified, `python3 scripts/garnet_mit_readiness_status.py` exit 0, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo fmt --all -- --check` exit 0, and `git diff --check` / `git diff --cached --check` exit 0. Honest scope: Windows and WSL domain execution/reproduction only; WSL is portability, not Linux seccomp/OS-sandbox enforcement; no Linux desktop/Tauri GUI launch, Wasmtime fuel, native package-format, production, or v1.0 claim.
- [2026-06-03 09:10 CDT] REVIEW PR#334 — Grep Loop self-audit reached **5/5** on S111's honest scope before merge. Checks covered changed-file inventory, committed Windows + WSL proof manifest verification, PR-body dogfood headings (local `check_dogfood_pr_body.py` pass; remote `PR dogfood evidence` pass), overclaim scan for seccomp/OS-sandbox/Wasmtime fuel/Linux desktop/Tauri GUI/production/v1.0 wording (only intended negative/non-claim boundary lines found), readiness no-regression **90.6%**, workspace tests/clippy/fmt/diff-check, and WSL labeling as execution/portability only. Remote CI was still running when this ledger line was added; do not merge until all required checks are green on the final head.
- [2026-06-03 09:18 CDT] MERGED PR#334 (squash `7e538bf` on `origin/main`) — S111 domain-proof reproduction landed via the Chrome Work profile after remote CI completed green (26 pass / 2 expected skipped release-publish rows). Scope remains Windows + WSL domain-matrix execution evidence; WSL is execution/portability only and still not Linux seccomp, OS-sandbox enforcement, Wasmtime fuel, or Linux desktop/Tauri GUI launch proof.
- [2026-06-03 09:22 CDT] STARTED agent-win-codex/s110-windows-wsl-ultrapunch-repro — fresh branch from `origin/main` `7e538bf` after S111 merged. Baseline before edits: readiness **90.6%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope: reproduce the S104 ultrapunch accept/reject loop on Windows and WSL, commit manifest-backed proof bundles under `proofs/windows/ultrapunch/` and `proofs/linux/repro/`, and keep WSL labeled as portability-repro rather than Linux enforcement. Plan at `.agent/plans/win-codex-S110-windows-wsl-ultrapunch-repro-plan.md`.
- [2026-06-03 09:37 CDT] PR-OPEN PR#335 (`Navigata1:agent-win-codex/s110-windows-wsl-ultrapunch-repro` -> `Island-Dev-Crew:main`, fork PR). S110 local proof is green: focused recorder test pass, readiness reporter tests pass, readiness no-regression **90.8%** with `windows_wsl_ultrapunch_repro` now committed/verified, Windows bundle under `proofs/windows/ultrapunch/windows-ultrapunch-repro-20260603-0927/`, WSL bundle under `proofs/linux/repro/wsl-ultrapunch-repro-20260603-0929/`, both bundle manifests verified, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo fmt --all -- --check` exit 0, `git diff --check` / `git diff --cached --check` exit 0, and PR body dogfood validation exit 0. Remote CI was queued at PR open; do not merge until all required checks are green on the final head. Honest scope: Windows + WSL ultrapunch reproduction; WSL is portability-repro only, not Linux seccomp, OS-sandbox enforcement, Wasmtime fuel, Linux desktop/Tauri GUI launch, production, or v1.0 proof.
- [2026-06-03 09:47 CDT] REVIEW PR#335 — Grep Loop reached **5/5** on the S110 reproduction scope. Checks covered changed-file inventory (55 files), corrected PR body dogfood headings (local validator pass; remote `PR dogfood evidence` pass), committed bundle verification for both Windows and WSL summaries/manifests, readiness no-regression **90.8%**, focused recorder/reporter tests, workspace tests/clippy/fmt/diff-check, overclaim scan review (only intended negative/honest-scope lines and narrow `enforced-kernel` rejection wording), and remote CI on head `392e13f` fully green (CodeQL, dogfood, determinism, Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, package smokes, security all pass; release/publish rows skipped as expected). Ready for authenticated Chrome Work-profile squash merge after this review ledger record gets a final green head.
- [2026-06-03 09:57 CDT] MERGED PR#335 (squash `42bae51` on `origin/main`) — S110 ultrapunch reproduction landed via the Chrome Work profile after final remote CI completed green on head `1eedbd7` (all required checks pass; release/publish rows skipped as expected). Scope remains Windows + WSL accept/reject reproduction; WSL is portability-repro only and still not Linux seccomp, OS-sandbox enforcement, Wasmtime fuel, Linux desktop/Tauri GUI launch, production, or v1.0 proof. `.dogfood/goal.json` marks S110 merged at confidence 5 in the follow-up state-record branch because the merge SHA/time did not exist until after PR#335 merged.
- [2026-06-03 10:12 CDT] STARTED agent-win-codex/s117-windows-wsl-studio-smoke — fresh branch from `origin/main` `023f358` after S110/S111 and state-record PR#336 merged. Baseline before edits: readiness **90.8%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is a narrow Windows/WSL Studio proof increment for the S117 package-pipeline runway: record Windows Tauri `--studio-smoke` evidence and WSL command-contract portability evidence, while keeping Linux desktop/Tauri GUI launch, signing, winget, Windows ARM64, production, and v1.0 claims open.
- [2026-06-03 10:33 CDT] PR-OPEN PR#337 (`Navigata1:agent-win-codex/s117-windows-wsl-studio-smoke` -> `Island-Dev-Crew:main`, fork PR). Local S117-increment proof is green: `scripts/smoke_garnet_studio_windows_wsl.py --gate` verifies committed Windows + WSL Studio smoke bundles, readiness no-regression is **90.9%** with `windows_wsl_studio_smoke=verified`, focused recorder/readiness/status/shell tests pass, Studio frontend `npm run build` passes, Tauri backend `cargo test --manifest-path apps/garnet-studio/src-tauri/Cargo.toml` passes, workspace tests/clippy/fmt/diff-check pass, and the PR body dogfood validator passes for 29 changed files. Honest scope: Windows Tauri `--studio-smoke` plus WSL command-contract portability only; no Linux seccomp, OS-sandbox enforcement, Linux desktop GUI launch, native Linux package, signed MSI, winget, Windows ARM64, production, or v1.0 claim. Remote CI still required before merge.
- [2026-06-03 10:40 CDT] REVIEW PR#337 — Grep Loop reached **5/5** on the S117-increment scope. Checks covered changed-file inventory (29 files), committed proof manifest gate, readiness no-regression **90.9%**, focused tests, Studio frontend/backend tests, workspace tests, clippy, fmt, diff-check, PR body dogfood validation, overclaim scan (only intended guardrail strings and `forbidden_claim` fields in captured status JSON), and remote CI on head `500ffdd` fully green (PR dogfood evidence, CodeQL, determinism, Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, package build/smokes, security all pass; release/publish rows skipped as expected). Ready for authenticated Chrome Work-profile squash merge after this REVIEW ledger commit gets a final green head.
- [2026-06-03 10:50 CDT] MERGED PR#337 (squash `ba97c1a` on `origin/main`) - S117-increment Windows/WSL Studio smoke proof landed via authenticated Chrome Work-profile merge after final remote CI completed green on head `754304b` (28 checks: PR dogfood evidence, CodeQL, determinism, Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, package build/smokes, and security all pass; release/publish rows skipped as expected). Scope remains Windows Tauri `--studio-smoke` plus WSL command-contract portability only; WSL is not Linux seccomp, OS-sandbox enforcement, Wasmtime fuel, Linux desktop GUI launch, native Linux package proof, signed MSI, winget, Windows ARM64, production, or v1.0 readiness. Full S117 package-pipeline readiness remains pending.
- [2026-06-03 11:08 CDT] STARTED agent-win-codex/s117-linux-wsl-deb-package-proof — fresh branch from `origin/main` `41a22be` after the S117 Studio smoke state record merged. Baseline before edits: readiness **90.9%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is another narrow S117 package-pipeline increment: record a WSL Linux Tauri `.deb` package build, inspect it with `dpkg-deb`, and run the Linux binary's non-GUI `--studio-smoke`; keep Linux desktop GUI install/launch, Linux seccomp/OS-sandbox enforcement, signing, winget, Windows ARM64, production, and v1.0 claims open. Plan at `.agent/plans/win-codex-S117-linux-wsl-deb-package-proof-plan.md`.
- [2026-06-03 11:25 CDT] PR-OPEN PR#339 (`Navigata1:agent-win-codex/s117-linux-wsl-deb-package-proof` -> `Island-Dev-Crew:main`, fork PR). Local S117 package increment proof is green: `scripts/smoke_garnet_studio_linux_wsl_deb.py --gate` verifies the committed WSL Linux `.deb` package-build bundle under `proofs/linux/execution/studio-package/`, focused recorder/readiness/status tests pass, readiness no-regression is **91.1%** with `linux_wsl_studio_deb_package=verified` and `windows_linux_distribution=75.0% active-partial`, `cargo fmt --all -- --check`, `cargo test --workspace --no-fail-fast`, `cargo clippy --workspace --all-targets -- -D warnings`, `git diff --check`, `git diff --cached --check`, and local PR-body dogfood validation all pass. Honest scope: WSL Linux `.deb` build + `dpkg-deb` inspection + non-GUI `--studio-smoke` only; no Linux desktop GUI install/launch, clean Linux install, Linux seccomp, OS-sandbox enforcement, signing/SBOM, winget, Windows ARM64, production, or v1.0 claim. Remote CI is required before merge.
- [2026-06-03 11:35 CDT] REVIEW PR#339 — Grep Loop reached **5/5** on the S117 WSL `.deb` package-build scope. Initial Linux CI correctly caught a manifest portability bug (Windows backslashes in `MANIFEST.sha256`); fixed with POSIX manifest paths plus a focused regression, then re-ran Windows and WSL proof/status tests locally. Final remote CI on head `2d9f523` is green: PR dogfood evidence, agent documentation contracts, CodeQL, determinism, Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, package build/smokes (`smoke-deb`/`smoke-rpm`), cargo audit/deny, VSIX builds, and security/SBOM pass; release/publish rows skipped as expected. Ready for authenticated Chrome Work-profile squash merge after this REVIEW ledger commit gets a final green head.
- [2026-06-03 11:44 CDT] MERGED PR#339 (squash `7f0d14d` on `origin/main`) - S117-increment WSL Linux `.deb` package-build proof landed via authenticated Chrome Work-profile merge after final remote CI completed green on head `f4637ab` (28 checks passed: PR dogfood evidence, agent documentation contracts, CodeQL, determinism, Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, package build/smokes, audit/deny, VSIX, and security/SBOM; release/publish rows skipped as expected). Scope remains WSL Linux `.deb` build + `dpkg-deb` inspection + non-GUI `--studio-smoke` only; it is not Linux desktop GUI install/launch, clean Linux install, Linux seccomp, OS-sandbox enforcement, signing/SBOM, winget, Windows ARM64, production, or v1.0 readiness. Full S117 package-pipeline readiness remains pending.
- [2026-06-03 11:59 CDT] STARTED agent-win-codex/s117-linux-wsl-deb-install-proof - fresh branch from `origin/main` `963bc58` after PR#340 merged the S117 WSL `.deb` package-build state record. Baseline before edits: readiness **91.1%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is a further narrow S117 package-pipeline increment: record WSL `.deb` install/extract command evidence using the already-built Tauri Linux package, verify the installed/extracted binary can run `--studio-smoke`, and keep the claim below Linux desktop GUI launch, clean Linux install, Linux seccomp/OS-sandbox enforcement, signing/SBOM, winget, Windows ARM64, production, or v1.0 readiness. Plan follows at `.agent/plans/win-codex-S117-linux-wsl-deb-install-proof-plan.md`.
- [2026-06-03 12:15 CDT] PR-OPEN PR#341 (`Navigata1:agent-win-codex/s117-linux-wsl-deb-install-proof` -> `Island-Dev-Crew:main`, fork PR). Local S117 install/extract increment proof is green: `scripts/smoke_garnet_studio_linux_wsl_deb_install.py --gate` verifies the committed WSL `.deb` extract bundle under `proofs/linux/execution/studio-package-install/`, WSL runs the same gate and focused tests, readiness no-regression is **91.3%** with `linux_wsl_studio_deb_install=verified` and `windows_linux_distribution=76.0% active-partial`, focused recorder/readiness/status tests pass, `cargo fmt --all -- --check`, `cargo test --workspace --no-fail-fast`, `cargo clippy --workspace --all-targets -- -D warnings`, `git diff --cached --check`, and local PR-body dogfood validation all pass. Honest scope: WSL `.deb` extract + extracted-binary non-GUI `--studio-smoke` only; no Linux desktop GUI install/launch, clean Linux install, privileged system package install, Linux seccomp, OS-sandbox enforcement, signing/SBOM, winget, Windows ARM64, production, or v1.0 claim. Remote CI is required before merge.
- [2026-06-03 12:22 CDT] REVIEW PR#341 — Grep Loop reached **5/5** on the S117 WSL `.deb` install/extract scope. Checks covered changed-file inventory (35 files), committed proof manifest gate on Windows and WSL, readiness no-regression **91.3%** with `linux_wsl_studio_deb_install=verified` and `windows_linux_distribution=76.0% active-partial`, focused recorder/status/readiness tests, workspace tests, clippy, fmt, staged diff-check, PR body dogfood validation, overclaim scan for Linux desktop GUI / clean Linux install / privileged system install / seccomp / OS-sandbox / production / v1.0, and remote CI on head `624a343` fully green (PR dogfood evidence, agent documentation contracts, CodeQL, determinism, Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, package build/smokes, audit/deny, VSIX, and security/SBOM pass; release/publish rows skipped as expected). Ready for authenticated Chrome Work-profile squash merge after this REVIEW ledger commit gets a final green head.
- [2026-06-03 12:32 CDT] MERGED PR#341 (squash `03fdd2e` on `origin/main`) — S117-increment WSL Linux `.deb` install/extract proof landed via authenticated Chrome Work-profile merge after final remote CI completed green on head `313a259` (PR dogfood evidence, agent documentation contracts, CodeQL, determinism, Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, package build/smokes, audit/deny, VSIX, and security/SBOM pass; release/publish rows skipped as expected). Scope remains WSL `.deb` extraction plus extracted-binary non-GUI `--studio-smoke` only; it is not Linux desktop GUI install/launch, clean Linux install, privileged system package install, Linux seccomp, OS-sandbox enforcement, signing/SBOM, winget, Windows ARM64, production, or v1.0 readiness. Full S117 package-pipeline readiness remains pending.
- [2026-06-03 12:54 CDT] STARTED agent-win-codex/s117-linux-wsl-rpm-package-proof — fresh branch from `origin/main` `c7f7a78` after PR#342 recorded the `.deb` install/extract merge state. Baseline before edits: readiness **91.3%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is another narrow S117 package-pipeline increment: record a WSL-driven Tauri Linux `.rpm` package build/inspection/extract command-smoke if RPM tooling can be installed in WSL, while keeping Linux desktop GUI install/launch, clean Linux install, Linux seccomp/OS-sandbox enforcement, signing/SBOM, winget, Windows ARM64, production, and v1.0 claims open. Plan follows at `.agent/plans/win-codex-S117-linux-wsl-rpm-package-proof-plan.md`.
- [2026-05-24 16:54 CDT] STARTED agent-win-codex/s16-rowan-lsp-precision — fresh branch from origin/main `09d6703`; S15-Compare has selected rowan `garnet-cst`, so S16 is unblocked. Baseline green before edits: readiness 78.8%, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Plan at `.agent/plans/win-codex-S16-plan.md`; proceeding with rowan-backed LSP migration while preserving #221 as migration oracle.
- [2026-05-24 18:23 CDT] PR-OPEN PR#230 — branch `agent-win-codex/s16-rowan-lsp-precision` pushed at `09b090c`; local S16 dogfood green and readiness reporter moved 78.8% -> 79.6%. Entering review/CI loop; S16 is not merged until PR#230 is green and reviewed.
- [2026-05-24 18:33 CDT] REVIEW PR#230 — first remote CI pass was green except `PR dogfood evidence`; root cause was missing required readiness-sensitive PR body headings, not code/test failure. PR body updated and locally validated with `scripts/check_dogfood_pr_body.py`; this ledger update retriggers remote CI against the corrected PR body. Not merged.
- [2026-05-24 18:39 CDT] REVIEW PR#230 — Grep Loop pass 1 returned 3/5 confidence with two fixable findings: VS Code refactor command filtered QuickFix while the server emitted RefactorRewrite, and the precision smoke verified semantic-token legend but not emitted category payload. Patched both; focused local proof passes (`cargo test -p garnet-lsp --no-fail-fast`, `npm run package`, `scripts/smoke_garnet_lsp_precision.py`). Not merged.
- [2026-05-24 18:46 CDT] MERGED PR#230 (squash `d539192` on origin/main) — S16 landed after Grep Loop pass 2 reached 5/5 and remote CI completed green (27 success / 2 skipped / 0 fail). `editor_lsp_precision` is verified in the readiness reporter at 79.6% overall; deferred scope remains safe-mode precision, cross-package rename, per-project semantic-token themes, and Marketplace/OpenVSX publication.
- [2026-05-25 23:09 CDT] STARTED agent-win-codex/wls-domain-proof-matrix — post-S16 Windows/Linux Studio hardening lane by direct Jon request. Fresh branch from `origin/main` `2d655a0`; baseline before edits: readiness 80.4%, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is repo-owned Windows/Linux Studio domain proof: a cross-platform parse/check/run evidence matrix for the canonical MVP + agentic examples, wired into the Tauri shell and readiness/status reporters without claiming Linux package, Windows ARM64, signing, or winget completion.
- [2026-05-25 23:27 CDT] BLOCKED agent-win-codex/wls-domain-proof-matrix — implementation committed (`b482b95`) and branch pushed; `gh` is authenticated as `Navigata1`, but GitHub reports `viewerPermission: READ` / `push:false`, and both Chrome PR creation plus `gh pr create` return `must be a collaborator`. Local dogfood remains green: readiness 81.0%, workspace test/clippy exit 0, Windows + WSL domain matrices 20/20 cases and 60/60 commands.
- [2026-05-25 23:43 CDT] PR-OPEN PR#232 — upstream PR opened from `Navigata1:agent-win-codex/wls-domain-proof-matrix` into `Island-Dev-Crew/garnet:main` after the same-repo PR path stayed blocked by collaborator permissions. Ready for Grep Loop review; no merge claim yet.
- [2026-05-26 01:08 CDT] REVIEW PR#232 — rebased on `origin/main` `b5cfcbf` after S19 merged; Grep Loop final pass reached 5/5 with no findings, and remote CI completed green on head `2034adc` (Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, CodeQL, audit/deny/SBOM, determinism, agentic dogfood matrix, VSIX, Linux packages, deb/rpm smokes). Merge still requires target-repo write permission; no MERGED claim until GitHub records the PR as merged.
- [2026-05-26 09:22 CDT] STARTED agent-win-codex/s22-stdlib-memory — **S22 (post-S21 stdlib + Mnemos runtime completion, Jon-directed):** branched from fresh `origin/main` (`edba6f3`, PR#236/S21 merged). Baseline before edits is green: readiness **82.4%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is the S21-deferred surface: `std::json` / `std::regex` / `std::uuid` / `std::env` / `std::process` / `std::log` interpreter dispatch, live Garnet-callable `memory::` constructors returning Mnemos handle Values, a runnable novel-composition proof, and a new readiness lane. This is post-v0.7/Jon-directed work; the S15-S19 crate lock is no longer active, but I will keep edits narrowly scoped to `garnet-interp-v0.3`, examples, focused scripts/tests, and section-scoped docs/status. Plan at `.agent/plans/win-codex-S22-plan.md`.
- [2026-05-26 09:45 CDT] PR-OPEN PR#237 (`Navigata1:agent-win-codex/s22-stdlib-memory` → `island-dev-crew:main`, fork PR — cached credential is `Navigata1`/fork-only). Local S22 proof is green: `cargo build -p garnet-cli`, S22 source-level integration 4/4, `stdlib_bridge` focused tests, `novel_05` deterministic run, novel-composition harness 5/5, readiness no-regression **82.9% / 34 lanes**, fmt/workspace-test/clippy/doc(`-D warnings`)/diff-check all exit 0. Entering Grep Loop + CI; no remote-green or merge claim yet.
- [2026-06-01 04:51 CDT] PR-OPEN PR#317 (`Navigata1:agent-win-codex/s93-bounded-loop-verifier` → `Island-Dev-Crew:main`, fork PR — local CLI token remains fork-only for push; Chrome Work profile has org merge authority). S93 adds the static bounded-loop verifier for safe / `@bounded` scope without claiming Wasmtime fuel, runtime loop metering, VM enforcement, or OS sandbox enforcement. Local proof is green: bounded-loop checker tests 9/9, CLI bounded-loop tests 3/3, conformance skeleton 22/22, status gate `ok: true`, agent contracts green, readiness **89.1%**, full workspace test green, and clippy `-D warnings` green. Desktop evidence bundle: `C:\Users\IslandDevCrew\Desktop\dogfood\garnet-s93-bounded-loop-verifier-20260601-044648` with `MANIFEST.sha256`. Entering CI / merge loop; no remote-green or merge claim yet.
- [2026-06-01 04:58 CDT] MERGED PR#317 (squash `fef5b08` on origin/main) — S93 landed. CI was green before merge and the shipped scope remains static-only bounded-loop verification for safe / `@bounded` code; no Wasmtime fuel, runtime metering, VM enforcement, or OS sandbox claim is made.
- [2026-06-01 05:06 CDT] STARTED agent-win-codex/s94-paper-vi-exp1-llm-pass1 — fresh branch from S93-merged `origin/main` (`fef5b08`). Baseline before edits is green: readiness **89.1%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is Paper VI Exp 1 harness wiring behind an explicit provider flag, plus provider-free/fixture proof and honest `pending-credentials` / `pending-infra` status when real LLM credentials or the full 500-task corpus are absent. Plan at `.agent/plans/win-codex-S94-plan.md`.
- [2026-06-01 05:20 CDT] PR-OPEN PR#318 (`Navigata1:agent-win-codex/s94-paper-vi-exp1-llm-pass1` → `Island-Dev-Crew:main`, fork PR). S94 local proof is green: focused status tests 6/6, S94 status gate `ok: true`, readiness no-regression **89.3%** with `paper_vi_exp1_provider_gated_harness`, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, and `git diff --check` exit 0. Desktop evidence bundle: `C:\Users\IslandDevCrew\Desktop\dogfood\garnet-s94-paper-vi-exp1-llm-pass1-20260601-051545` with `MANIFEST.sha256`. Honest scope: provider-gated harness only; no provider-backed pass@1 measurement, full 500-task corpus, hidden-test scorer, or statistical result is claimed.
- [2026-06-01 05:27 CDT] MERGED PR#318 (squash `f24c0ed` on origin/main) — S94 landed after remote CI green (28 checks: required jobs pass; release/publish skipped as expected). The shipped scope remains provider-gated harness proof only; no provider-backed pass@1 measurement is claimed.
- [2026-06-01 05:32 CDT] STARTED agent-win-codex/s95-paper-vi-exp3-5k-loc — fresh branch from S94-merged `origin/main` (`f24c0ed`). Baseline before edits is green: readiness **89.3%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is a reproducible Paper VI Exp 3 5K-LOC rerun harness and status gate; h3a remains honestly unresolved unless provider-backed measured rows exist. Plan at `.agent/plans/win-codex-S95-plan.md`.
- [2026-06-01 05:43 CDT] PR-OPEN PR#319 (`Navigata1:agent-win-codex/s95-paper-vi-exp3-5k-loc` → `Island-Dev-Crew:main`, fork PR). S95 local proof is green: focused status tests 5/5, S95 status gate `ok: true` with ten generated snapshots (min 5012 LOC, 50120 total), twenty provider-free rows, zero measured rows, readiness no-regression **89.6%**, generated corpus source check 10/10 (`garnet check` clean), `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, and `git diff --check` exit 0. Desktop evidence bundle: `C:\Users\IslandDevCrew\Desktop\dogfood\garnet-s95-paper-vi-exp3-5k-loc-20260601-054008` with `MANIFEST.sha256`. Honest scope: no new 5K h3a measurement is claimed; provider-backed runtime rows remain pending.
- [2026-06-01 05:51 CDT] MERGED PR#319 (squash `3969093` on origin/main) — S95 landed after remote CI green (28 checks: required jobs pass; release/publish skipped as expected). The shipped scope remains a deterministic 5K-LOC Exp 3 rerun harness with provider-free rows only; no new provider-backed h3a timing measurement is claimed.
- [2026-06-01 05:57 CDT] STARTED agent-win-codex/s96-linear-effect-safe-mode — fresh branch from S95-merged `origin/main` (`3969093`). Baseline before edits is green: readiness **89.6%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is a narrow static linear/effect safe-mode seed in `garnet-check-v0.3`: reject authority-bearing safe helper functions that lack an explicit ownership-qualified parameter boundary, while keeping VM/runtime enforcement and whole-language proof out of claim. Plan at `.agent/plans/win-codex-S96-plan.md`.
- [2026-06-01 06:06 CDT] PR-OPEN PR#320 (`Navigata1:agent-win-codex/s96-linear-effect-safe-mode` → `Island-Dev-Crew:main`, fork PR). S96 local proof is green: focused checker tests 5/5, status tests 3/3, S96 status gate `ok: true`, readiness no-regression **89.8%** with `linear_effect_safe_mode_seed`, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo fmt --all --check` exit 0, and `git diff --check` exit 0. Desktop evidence bundle: `C:\Users\IslandDevCrew\Desktop\dogfood\garnet-s96-linear-effect-safe-mode-20260601-060332` with `MANIFEST.sha256`. Honest scope: first static linear/effect seed only; no whole-language, VM/runtime, or OS sandbox enforcement claim.
- [2026-06-01 06:16 CDT] REVIEW PR#320 — Grep Loop reached **5/5** on the shipped S96 scope. Checks covered changed-file inventory, PR body dogfood headings, overclaim scan for whole-language / VM-runtime / OS-sandbox wording, fatal `check.linear_effect` severity/ok classification, readiness lane, local evidence bundle, and remote CI. Remote CI is fully green on head `935eb54` (all required jobs pass; release/publish jobs skipped as expected), `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. Ready for Chrome work-profile squash merge.
- [2026-06-01 06:24 CDT] MERGED PR#320 (squash `b6a6a49` on origin/main) - S96 landed through the authenticated Chrome work profile after final CI showed 29 checks passed. The shipped scope remains a narrow static linear/effect safe-mode seed; no whole-language linear typing, VM/runtime capability enforcement, or OS sandbox enforcement is claimed.
- [2026-06-01 06:30 CDT] STARTED agent-win-codex/s97-provenance-seal-chain - fresh branch from S96-merged `origin/main` (`b6a6a49`). Baseline before edits is green: readiness **89.8%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is the S97 provenance seal chain: bind self-declared agent/model/prompt attestations to the current seal subject/source digests with machine-checkable validation, while explicitly preserving that this does not independently prove the model executed the prompt or that the declared agent/tool list is complete. Plan at `.agent/plans/win-codex-S97-plan.md`.
- [2026-06-01 06:46 CDT] PR-OPEN PR#321 (`Navigata1:agent-win-codex/s97-provenance-seal-chain` -> `Island-Dev-Crew:main`, fork PR). S97 local proof is green: provenance-chain integration tests 4/4, status tests 3/3, S97 status gate `ok: true`, readiness no-regression **90.0%** with `provenance_seal_chain`, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo fmt --all --check` exit 0, and `git diff --check` exit 0. Desktop evidence bundle: `C:\Users\IslandDevCrew\Desktop\dogfood\garnet-s97-provenance-seal-chain-20260601-064007` with `MANIFEST.sha256`. Honest scope: deterministic binding of self-declared agent/model/prompt metadata only; no independent model-origin, named-agent, complete tool-list, or supply-chain-signing proof is claimed.
- [2026-06-01 06:54 CDT] REVIEW PR#321 — Grep Loop reached **5/5** on the shipped S97 scope. Checks covered changed-file inventory, PR body dogfood headings, overclaim scan for independent-origin / named-agent / complete-tool-list / supply-chain-signing claims, focused provenance chain gate, readiness no-regression, local evidence bundle, and remote CI on head `ea27fb3`. Remote CI was fully green (required jobs pass; release/publish jobs skipped as expected), `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. Ready for Chrome work-profile squash merge after this ledger-only review record gets its final green head.
- [2026-06-01 07:02 CDT] MERGED PR#321 (squash `611366b` on origin/main) - S97 landed through the authenticated Chrome work profile after final CI showed 29 checks passed. The shipped scope remains deterministic binding of self-declared agent/model/prompt metadata; no independent model-origin, named-agent, complete tool-list, or supply-chain-signing proof is claimed.
- [2026-06-01 07:08 CDT] STARTED agent-win-codex/s98-capability-manifest-standard - fresh branch from S97-merged `origin/main` (`611366b`). Baseline before edits is green: readiness **90.0%**, `cargo test --workspace --no-fail-fast` exit 0, and `cargo clippy --workspace --all-targets -- -D warnings` exit 0. Scope is the S98 capability-manifest standard seed: deterministic reference profile, test vectors, docs/RFC alignment, and status gate, while preserving that no external body has adopted the standard and declared-capability manifests do not prove absence of undeclared authority. Plan at `.agent/plans/win-codex-S98-plan.md`.
- [2026-06-01 07:19 CDT] PR-OPEN PR#322 (`Navigata1:agent-win-codex/s98-capability-manifest-standard` -> `Island-Dev-Crew:main`, fork PR). S98 local proof is green: focused capability-manifest standard tests 3/3, status tests 3/3, S98 status gate `ok: true`, governance/RFC gate `ok: true`, readiness no-regression **90.2%** with `capability_manifest_standard_seed`, `cargo test --workspace --no-fail-fast` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo fmt --all --check` exit 0, and `git diff --check` exit 0. Desktop evidence bundle: `C:\Users\IslandDevCrew\Desktop\dogfood\garnet-s98-capability-manifest-standard-20260601-071514` with `MANIFEST.sha256`. Honest scope: draft/reference seed only; no OWASP/LF adoption, multi-language ecosystem, undeclared-authority absence proof, or VM/runtime enforcement claim.
- [2026-06-01 07:24 CDT] REVIEW PR#322 — Grep Loop reached **5/5** on the shipped S98 scope. Checks covered changed-file inventory, PR body dogfood headings, overclaim scan for standards-adoption / multi-language-ecosystem / undeclared-authority-proof / VM-enforcement wording, focused S98 gate, governance/RFC regression gate, readiness no-regression, local evidence bundle, and remote CI on head `b24db83`. Remote CI was fully green (required jobs pass; release/publish jobs skipped as expected), `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. Ready for Chrome work-profile squash merge after this ledger-only review record gets its final green head.
- [2026-06-01 07:34 CDT] MERGED PR#322 (squash `05b18e5` on origin/main) - S98 landed through the authenticated Chrome work profile after final CI showed 29 checks passed. This closes the S91-S98 substrate lane at readiness **90.2%**. The shipped scope remains a draft capability-manifest reference seed; no standards-body adoption, multi-language ecosystem, absence-of-undeclared-authority proof, VM/runtime enforcement, Linux seccomp, or Wasmtime enforcement is claimed.
- [2026-05-26 09:52 CDT] REVIEW PR#237 — Grep Loop pass 1 found two fix-before-merge items: (1) `std::env` bridge could panic through host env APIs on invalid keys/values or non-Unicode entries, and (2) the committed diff had an extra blank line at EOF in `novel_05`. Patched env validation + OS-string vars handling, added `s22_env_invalid_inputs_surface_runtime_errors`, removed the EOF blank line, and re-ran local proof: fmt, targeted env test, S22 integration 4/4, `stdlib_bridge` 30 tests, `novel_05`, novel harness 5/5, workspace test, clippy, doc(`-D warnings`), readiness no-regression all exit 0. Will push after commit and re-check base-to-head diff + CI.
- [2026-05-26 09:59 CDT] REVIEW PR#237 — Grep Loop pass 2 returned **no findings**; reviewer confidence 4.5/5 because they did targeted pass only, while local full proof already covers workspace test/clippy/doc. Remote CI is fully green on head `962a27e`: 27 pass / 0 fail / 2 skip, including Windows/macOS/Ubuntu cargo tests, clippy, rustfmt, docs, determinism compare, package smokes, CodeQL, dogfood evidence, and agentic dogfood. `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. My readiness confidence is **5/5 on the shipped S22 scope**; merge still depends on GitHub accepting the authenticated account's permission.
- [2026-05-26 10:05 CDT] BLOCKED PR#237 merge — final remote CI on head `a5d880a` is green (27 pass / 0 fail / 2 skip; `mergeStateStatus=CLEAN`), but both merge paths are permission-blocked: `gh pr merge` returns `Navigata1 does not have the correct permissions to execute MergePullRequest`, and the GitHub connector returns `403 Resource not accessible by integration`. Authenticated Chrome shows the PR page with all checks passed and "Changes can be cleanly merged," but no merge button is available for this account. Ready for Jon/org-write squash merge; no MERGED claim.

### win-opus (S17)
- [2026-05-24 16:25 CDT] STARTED agent-win-opus/s17-stdlib-layers — branched off fresh `origin/main` (`09d6703`, S15 fully merged + S15-Compare recorded). Baseline re-verified on this base: `cargo clippy --workspace --all-targets -- -D warnings` exit 0; readiness 78.8% / 26 lanes; `--check-no-regression` exit 0 (workspace test re-running). Plan at `.agent/plans/win-opus-S17-plan.md`. S17 has no upstream dep. Checked what landed per Jon: S15 CST is in, but the parser still hard-errors on unknown annotations (`functions.rs:96`; no `Annotation::Stability`) — so `@stability`/`@uses`/`@migration` source syntax is **not yet parseable**. Filed a Handoff Request to mac-opus (below). Shipping the parser-independent bulk now (Layer Policy doc, ≥50 prims with registry stability+layer metadata, `@caps(env)` known-cap, registry-driven stability warnings, `garnet_stdlib_layer_gate.py` + `stdlib_layer_policy` lane); source-level `@stability` enforcement on user fns labeled pending-handoff.
- [2026-05-24 17:45 CDT] PR-OPEN PR#231 (`agent-win-opus/s17-stdlib-layers`) — S17: stdlib expansion + layer policy + `@stability`. Merged current `origin/main` (`d103525`, S16) into the branch; cross-cutting conflicts (CHANGELOG/CURRENT_STATE/dogfood/readiness/baseline/ledger) resolved keeping **both** slices' additive edits; `mergeable=MERGEABLE`. All local gates green on the merged tree: `fmt --check`, `clippy -D warnings`, `cargo test --workspace`, readiness `--check-no-regression`, conformance matrix, layer-gate (77 prims / 100% `@stability` / PASS) + its 9-test unittest, and `garnet check examples/mvp_*.garnet` → 0 new diagnostics across all 13. Readiness 79.6% → **80.4%** (28 lanes; baseline regenerated to full snapshot). Pre-existing Windows-only `scripts/` failures (shasum / cp1252 / exec-bit / SwiftPM) are unrelated to S17; Linux CI is the cross-cutting authority.
- [2026-05-24 17:50 CDT] REVIEW PR#231 — self-driven grep-loop vs PRD-C + the S17 contract. Done-criteria all met: ≥50 prims (77), ≥95% explicit `@stability` (100%), layer-gate reports numbers, `GARNET_STDLIB_LAYER_POLICY.md` exists + referenced from `CURRENT_STATE.md`, CHANGELOG `[Unreleased]` updated, baseline regenerated. Crate ownership clean (writes only in `garnet-stdlib` + `garnet-check-v0.3` + permitted cross-cutting). **Confidence 5/5 on the shipped scope**; the parser-handoff half (source-level `@stability`/`@uses`/`@migration` on user fns) is honestly labeled pending, not a defect. Per stream convention I do **not** self-merge — ready for Jon's merge call. CI: dogfood-evidence re-running against the corrected PR body on the next sync; matrix/clippy/deny/VSIX/SBOM in progress.
- [2026-05-24 18:35 CDT] REVIEW PR#231 — **CI fully GREEN** (`gh pr checks` = 27 pass / 0 fail / 2 skip); `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, not draft. Two CI-only issues found and fixed during review: (1) `cargo doc -D warnings` — ambiguous `[\`env\`]` intra-doc link (`env` is also the `env!` macro) → disambiguated with `mod@` (`3a921e1`); (2) agentic-matrix `check-advertised-safe-io-example` (expects "0 diagnostics") — bare `ok(...)`/`err(...)` builders were flagged because new `core::result::ok/err` share the name → `@stability` advisory now resolves **qualified-only** (`ddd7517`); `garnet check` on all 23 examples shows 0 stability diagnostics, qualified experimental calls still warn. Readiness 80.4% (28 lanes). **Ready for merge — awaiting Jon's call (no self-merge per convention).**
- [2026-05-24 18:45 CDT] MERGED PR#231 (squash `7bbca42` on origin/main) — S17 landed: Layer Policy doc, stdlib 24→77 prims (100% `@stability`), `@caps(env)`, qualified-only `@stability` advisory, `stdlib_layer_policy` lane (readiness → 80.4%, 28 lanes). Jon approved the squash-merge. **Unblocks mac-codex's S18** (hard-blocked on S17 MERGED) and the soft dep for S19's `@stability` tier. Open follow-up (separate small PR): wire user-fn `@stability`/`@uses`/`@migration` enforcement once mac-opus ships the parser annotation variants (Handoff Request above).
- [2026-05-25 09:10 CDT] STARTED agent-win-opus/s20-program-execution — **S20 (post-v0.7 dogfood extension, Jon-directed):** novel-composition example programs + a program-execution discovery harness + story doc. Branched off fresh `origin/main` (`3369bd3`; S15–S19 all merged + #232 Windows/Linux domain-proof matrix). Baseline green: `cargo test --workspace` exit 0, `cargo clippy -D warnings` exit 0, readiness **81.3% / 31 lanes**. Scope is strictly **additive** — NEW `examples/novel_*.garnet` composing ≥3 Paper-VI contributions each (capability-budget + memory-recall + agent pipeline + signed-blake3 provenance + determinism), a NEW `scripts/smoke_garnet_novel_compositions.py` (+ unittest) that proves each parses/checks/runs with deterministic asserted output, a story doc, and a new readiness lane. **No edits** to any owned crate (garnet-lsp/cst/parser/suggest-llm/garnet-lang/interp/vm/memory/cli) or win-codex's `smoke_garnet_studio_domain_matrix.py`; I only add new files + section-scoped cross-cutting. Plan at `.agent/plans/win-opus-S20-plan.md`.
- [2026-05-25 10:05 CDT] PR-OPEN PR#235 (`Navigata1:agent-win-opus/s20-program-execution` → `island-dev-crew:main`) — S20 novel-composition dogfood. **Note:** the cached Git credential is now `Navigata1` (fork-only; lacks `origin` write — `git push origin` 403s), so this is a **fork PR** (pushed to `fork`, cross-repo PR to the org). Three programs fuse 3–4 Paper-VI contributions each (governance 16 / verified provenance fingerprint / APPROVED quorum 4); `smoke_garnet_novel_compositions.py` + 8-test unittest prove check-clean + deterministic run. Additive only — no source crate or win-codex's domain matrix touched. Readiness 81.3% → 81.9% (32 lanes; baseline surgically extended, per-lane floors preserved). Local: harness 3/3, fmt/conformance/readiness-no-regression green; the 5 Windows-only `scripts/` failures (shasum/cp1252/exec-bit/swiftpm) are pre-existing + environmental (CI Linux is clean).
- [2026-05-25 10:18 CDT] REVIEW PR#235 — **CI green** (`gh pr checks` = 26 pass / 0 fail / 2 skip / 1 slow-infra pending); `mergeable=MERGEABLE`. Self-driven grep-loop vs the S20 contract + directive: all met (3 programs fusing ≥3 contributions, deterministic exact-output assertions, additive ownership, readiness up + granular, honest modeled-deterministically scope). **Confidence 5/5.** **Merge requires org-write** — the cached token is `Navigata1` (fork-only), so I cannot self-merge into `island-dev-crew/garnet`; ready for Jon (or an org-write account) to squash-merge #235.
- [2026-05-26 08:05 CDT] MERGED PR#235 (squash `0970a4c` on origin/main) — S20 landed (Jon merged it under an org-write account). Novel-composition dogfood is on main; readiness lane `novel_composition_dogfood` active.
- [2026-05-26 08:40 CDT] STARTED agent-win-opus/s21-interp-dispatch — **S21 (interpreter dispatch for the S17 Layer-0/1 prims, Jon-directed):** the v0.7 deferred line ("the new prims aren't interpreter-dispatched yet"). Branched off `origin/main` `0970a4c`; baseline `cargo test --workspace` + `clippy -D warnings` exit 0. **Takes on `garnet-interp-v0.3` under Jon's explicit direction** (post-v0.7; the slice-ownership lockdown was for the parallel S15–S19 build, now merged; garnet-interp is unowned — no active-agent conflict). Approach: `eval_path` now resolves the **fully-qualified name first** (backward-compatible) so prims bind under `core::math::sqrt` etc. without colliding with bare prelude builtins (`map`/`ok`); then qualified bridges for `core::math` (6, dispatches stdlib), `core::cmp` (4, Value-level), `core::iter` map/filter/fold/take/drop/enumerate (6, **higher-order via call_value** — real first-class-function combinators from Garnet), `std::base64` (2, dispatches stdlib). Verified live: `core::math::sqrt(16.0)→4`, `core::iter::map([1,2,3,4], double)→[2,4,6,8]`, `std::base64::encode("hi")→"aGk="`. Also adds a Mnemos × stdlib Rust combination smoke. Deferred (labeled): `std::json`/`regex`/`uuid`/`env`/`process`/`log` dispatch + a full managed-mode `memory::` prim family → S22. Plan at `.agent/plans/win-opus-S21-plan.md`.
- [2026-05-26 10:05 CDT] PR-OPEN PR#236 (`Navigata1:agent-win-opus/s21-interp-dispatch` → `island-dev-crew:main`, fork PR — cached cred is `Navigata1`/fork-only). 18 newly-runnable prims via qualified dispatch; `eval_path` qualified-first; `core::iter` higher-order via `call_value`; Mnemos × stdlib Rust combo test (4 kinds). New `interp_stdlib_dispatch` lane (33 lanes); baseline surgically extended (floors preserved). Local gates ALL green: fmt/clippy/test/doc(`-D warnings`)/readiness-no-regression/conformance + smoke 4/4 + 25 bridge + 2 Mnemos tests.
- [2026-05-26 10:25 CDT] REVIEW PR#236 — **CI fully GREEN** (`gh pr checks` = 27 pass / 0 fail / 2 skip); `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. Self-driven grep-loop vs the S21 plan + directive: all met (eval_path qualified-first backward-compatible; 18 prims dispatched incl higher-order iter verified live; Mnemos × stdlib combo green; readiness up + granular; additive ownership; honest scope labeled). **Confidence 5/5.** **Merge needs org-write** (token is `Navigata1`/fork-only) — ready for Jon to squash-merge #236.
- [2026-05-27 09:05 CDT] MERGED PR#236 (squash `edba6f3` on origin/main) — S21 landed (merged under an org-write account). `interp_stdlib_dispatch` lane active; the S21-deferred `std::json`/regex/uuid/env/process/log + `memory::` line was then closed by S22/PR#237 (`7f27f91`, win-codex), which is also merged. Ledger MERGED records for #236/#237 were backfilled here after confirming `git log origin/main`.
- [2026-05-27 09:20 CDT] STARTED agent-win-opus/s23-process-runtime — **S23 (Jon-directed; closes an S22 deferred line):** `std::process` structured argv + output capture. Branched off fresh `origin/main` (`7f27f91`, S22 merged). Baseline green: `cargo test --workspace` + `clippy -D warnings` exit 0, readiness **82.9% / 34 lanes**, `--check-no-regression` exit 0. Assessed PR#237/S22 first (build + 4/4 S22 dispatch tests + novel_05 deterministic + novel harness 5/5 + readiness no-regression) — **nothing left in S22**. S23 scope: add `std::process::spawn_args(program, [args])` (literal argv, no shell-split — spaces survive) and `std::process::output(program, [args])` (run-to-completion capturing stdout/stderr/exit-code), keeping `spawn`/`wait`/`exit_code` backward-compatible. Edits `garnet-stdlib` (process module + registry — win-opus-owned since S17) + `garnet-interp-v0.3` (bridge dispatch — taken under Jon's direction since S21) + new dispatch test + readiness lane + section-scoped cross-cutting. **No parser/CST change** (new prim names are plain identifiers → mac-opus's lane untouched). Plan at `.agent/plans/win-opus-S23-plan.md`.
- [2026-05-27 10:35 CDT] MERGED PR#238 (squash `cc69b60` on origin/main) — S23 landed. Fork PR (`Navigata1:agent-win-opus/s23-process-runtime` → `island-dev-crew:main`); CI fully green (27 pass / 2 skip / 0 fail), `mergeable=MERGEABLE`/`CLEAN`. Self-driven grep-loop 5/5 (scope faithful to the named S22 deferral; `spawn`/`wait`/`exit_code` unchanged; real behavioral tests incl. POSIX `printf "%s"` spaced-argv discriminator; in-lane). `process_runtime_completion` lane active (readiness 83.4% / 35 lanes). **Merged via the org-write Chrome work profile (browser control), since `Navigata1` is `push:false` on the org repo** — Jon authorized browser-merge for this goal.
- [2026-05-27 10:45 CDT] STARTED agent-win-opus/s24-log-filesink — **S24 (Jon-directed; closes the S22/S23/S17-deferred `std::log` file-sink line):** branched off fresh `origin/main` (`cc69b60`, S23 merged). Baseline green: readiness **83.4% / 35 lanes**, `--check-no-regression` exit 0. Scope: add `std::log::to_file(path, level, message)` to `garnet-stdlib` (log module + registry — win-opus-owned) — formats `[LEVEL] message` and appends it (create-if-missing) to a file, requiring `@caps(fs)` — plus `stdlib_bridge.rs` dispatch (arity 3) and a source-level write→read-back proof. **No parser/CST change** (`to_file` is a plain identifier). Plan at `.agent/plans/win-opus-S24-plan.md`.
- [2026-05-27 11:30 CDT] MERGED PR#239 (squash `3015ace` on origin/main) — S24 landed. Fork PR; CI fully green (27 pass / 2 skip / 0 fail), `mergeable=MERGEABLE`/`CLEAN`. Grep-loop 5/5 (faithful to the named deferral; formatters unchanged; real append/no-truncate/IO-error + write→read-back tests; in-lane). `log_file_sink_runtime` lane active (readiness 83.9% / 36 lanes). Merged via the org-write Chrome work profile (browser control).
- [2026-05-27 11:40 CDT] STARTED agent-win-opus/s25-host-effect-capstone — **S25 (Jon-directed; capstone of the S22–S24 arc):** branched off fresh `origin/main` (`3015ace`, S24 merged). Baseline green: readiness **83.9% / 36 lanes**, `--check-no-regression` exit 0. Scope is **additive composition + story**: a new `garnet-interp-v0.3/tests/host_effect_composition.rs` threading `std::process::output` (S23) → `std::log::to_file` (S24) → `memory::episodic` (S22) → `read_file` → `crypto::blake3` from a `@caps(proc, fs)` Garnet program, plus a deterministic `examples/novel_06_observability_provenance_pipeline.garnet` (novel harness → 6/6), the story doc section, and a `host_effect_composition` readiness lane. **No parser/CST/owned-crate source change** — only a new example + test + additive docs/scripts. Plan at `.agent/plans/win-opus-S25-plan.md`.
- [2026-05-27 12:05 CDT] MERGED PR#240 (squash `0f2b8f4` on origin/main) — S25 landed (this MERGED line is backfilled in the S26 branch, since S25 was the last slice of the prior goal and had no successor branch to carry it; merge confirmed via `git log origin/main` + PR#240 state MERGED). Fork PR; CI green (27 pass / 2 skip / 0 fail); grep-loop 5/5; merged via the org-write Chrome work profile. `host_effect_composition` lane active (readiness 84.3% / 37 lanes).
- [2026-05-27 12:15 CDT] STARTED agent-win-opus/s26-result-dispatch — **S26 (Jon-directed; continues the S21/S22 registry→runtime arc, new goal: S26–S30):** branched off fresh `origin/main` (`0f2b8f4`, S25 merged). Baseline green: readiness **84.3% / 37 lanes**, `--check-no-regression` exit 0. Checked what landed: no other agents' open PRs; main tip S25. Scope: dispatch the 6 registered-but-unrunnable `core::result` combinators (ok/err/map/and_then/or_else/unwrap_or) in `garnet-interp-v0.3/stdlib_bridge.rs` over `Result` Variants (higher-order via `call_value`; bound qualified to avoid the bare-`map` collision) + a source-level railway proof + readiness lane. **No parser/CST/stdlib-registry change** (registry already has the entries). Plan at `.agent/plans/win-opus-S26-plan.md`.
- [2026-05-27 12:55 CDT] MERGED PR#241 (squash `ef382fc` on origin/main) — S26 landed. Fork PR; CI green after two remediations handled WITHOUT merging over red: (1) a **real latent flake** — `stdlib_s24_dispatch`'s two tests shared a nanos+pid temp path and raced on macOS's coarse clock — fixed with a per-call `AtomicU64` (commit `8ce8859`); (2) a **transient `actions/cache@v5` infra failure** on the Windows job (test never ran) — re-ran that job via the org-write Chrome work profile (Actions UI → Re-run failed jobs). Final: 27 pass / 2 skip / 0 fail. Grep-loop 5/5. Merged via the work profile. `core_result_dispatch` lane active (readiness 84.7% / 38 lanes).
- [2026-05-27 13:05 CDT] STARTED agent-win-opus/s27-option-dispatch — **S27 (Jon-directed; sibling of S26):** branched off fresh `origin/main` (`ef382fc`, S26 merged). Baseline green: readiness **84.7% / 38 lanes**, `--check-no-regression` exit 0. Scope: dispatch the 5 registered `core::option` combinators (some/none/map/and_then/unwrap_or) over `Option` Variants in `stdlib_bridge.rs` (higher-order via `call_value`; bound qualified) + a source-level proof + readiness lane. **No parser/CST/stdlib-registry change.** Plan at `.agent/plans/win-opus-S27-plan.md`.
- [2026-05-27 13:35 CDT] MERGED PR#242 (squash `ce1d4b3` on origin/main) — S27 landed. Fork PR; CI green first try (27 pass / 2 skip / 0 fail) — the S26 `stdlib_s24` flake fix on main prevented recurrence. Grep-loop 5/5. Merged via the org-write Chrome work profile. `core_option_dispatch` lane active (readiness 85.1% / 39 lanes).
- [2026-05-27 13:45 CDT] STARTED agent-win-opus/s28-iter-completion — **S28 (Jon-directed; finishes the core:: registry→runtime arc):** branched off fresh `origin/main` (`ce1d4b3`, S27 merged). Baseline green: readiness **85.1% / 39 lanes**, `--check-no-regression` exit 0. Scope: dispatch the last 3 registered `core::iter` combinators (zip/collect/chain) in `stdlib_bridge.rs` (Value-level; collect expands a `Range` or materializes an Array) + a source-level proof composed with the S21 `fold` + readiness lane. With S26 (result) + S27 (option), this makes the full `core::` surface runnable. **No parser/CST/stdlib-registry change.** Plan at `.agent/plans/win-opus-S28-plan.md`.
- [2026-05-27 14:15 CDT] MERGED PR#243 (squash `980c65b` on origin/main) — S28 landed. Fork PR; first CI run hit a **transient `macos-cli-tarballs (aarch64)` "Native CLI smoke" broken-pipe** (EPIPE race in the smoke harness, not my code — out of lane to fix the CLI's SIGPIPE handling) → re-ran that job via the org-write Chrome work profile (Actions → Re-run failed jobs). Re-run green: 27 pass / 2 skip / 0 fail. Grep-loop 5/5. Merged via the work profile. `core_iter_completion` lane active (readiness 85.5% / 40 lanes). All 9 `core::iter` + result + option combinators now runnable.
- [2026-05-27 14:25 CDT] STARTED agent-win-opus/s29-stability-errors — **S29 (Jon-directed; the named v0.8 `@stability` error-level deferral):** branched off fresh `origin/main` (`980c65b`, S28 merged). Baseline green: readiness **85.5% / 40 lanes**, `--check-no-regression` exit 0. Scope is **garnet-check's stability surface only** (win-opus-owned since S17): add a FATAL `CheckError::StabilityError` variant + list it in `CheckReport::ok()`, and gate experimental/deprecated → error behind `GARNET_STABILITY_ERRORS` in `stability.rs` (frozen→info; default unchanged warning-level). `garnet-cli` already exits on `report.ok()`, so **no garnet-cli change**. Verified end-to-end: `GARNET_STABILITY_ERRORS=1 garnet check novel_04` → "stability error" exit 1; default → warning exit 0. **No parser/CST/stdlib/garnet-cli change.** Plan at `.agent/plans/win-opus-S29-plan.md`.
- [2026-05-27 14:50 CDT] MERGED PR#244 (squash `d301bed` on origin/main) — S29 landed. Fork PR; CI green (27 pass / 2 skip / 0 fail; one slow Windows test job, no failures). Grep-loop 5/5. Merged via the org-write Chrome work profile. `stability_error_enforcement` lane active (readiness 85.9% / 41 lanes).
- [2026-05-27 15:00 CDT] STARTED agent-win-opus/s30-functional-capstone — **S30 (Jon-directed; capstone of the S26-S28 functional-core arc — final slice of the S26–S30 goal):** branched off fresh `origin/main` (`d301bed`, S29 merged). Baseline green: readiness **85.9% / 41 lanes**, `--check-no-regression` exit 0. Scope is **additive composition + story**: `examples/novel_07_functional_core_pipeline.garnet` (iter→result→option railway, deterministic `final: 80`, novel harness → 7/7) + `garnet-interp-v0.3/tests/functional_core_composition.rs` (both Ok/Some + Err-recovered/None tracks → `[20,40,0,80,7]`) + story doc section + `functional_core_composition` readiness lane. **No parser/CST/owned-crate source change** — new example + test + additive docs only. Plan at `.agent/plans/win-opus-S30-plan.md`.
- [2026-05-27 15:05 CDT] FROM:win-opus TO:Jon — **S26–S30 goal complete after this slice merges.** The functional `core::` surface (result/option/iter — S26/S27/S28) is now fully interpreter-dispatched, error-level `@stability` is opt-in (S29), and S30 proves the functional core composes. **Ledger MERGED-line note:** as in prior goals, each slice's MERGED line is carried in the *next* slice's branch; S30 is the last, so its MERGED status will be evident from `git log origin/main` + PR#245 state (no successor branch to carry it). Two transient CI infra flakes were handled this goal WITHOUT merging over red — a real `stdlib_s24` macOS temp-path collision (fixed with an atomic counter in PR#241) and two transient re-runs (windows `actions/cache`, macOS aarch64 broken-pipe smoke) via the work-profile Actions UI.

### mac-codex (S18, S19)
- [2026-05-24 17:26 CDT] STARTED agent-mac-codex/s19-suggest-llm — planning S19 first from current origin/main `09d6703`; S18 remains blocked until win-opus/S17 is MERGED. Baseline green: readiness 80.6%, `cargo test --workspace --no-fail-fast`, `cargo clippy --workspace --all-targets -- -D warnings`.
- [2026-05-24 17:42 CDT] BLOCKED agent-mac-codex/s19-suggest-llm — S19 is feature-gated-source-ready locally: `garnet-suggest-llm` builds/tests with `--features llm`, provider-compatible Anthropic/OpenAI/Ollama request/response paths are covered by tests, repro logging and the Paper VI Exp 3 harness are present, determinism `--llm` guard is in CI, readiness moved 80.6% -> 80.7%, and workspace `cargo test`, workspace `cargo clippy`, scripts unittest discovery, `cargo deny check`, and `git diff --check` are green. Public dogfood `garnet check --suggest --llm ...` is blocked on the ledgered read-only `garnet-cli` / optional `garnet-check-v0.3` handoff. No PR opened; waiting for Jon approval plus handoff resolution before PR-open.
- [2026-05-24 17:50 CDT] BLOCKED agent-mac-codex/s19-suggest-llm — synced over origin/main `d103525` after S16 merged; S18 remains blocked because S17 is still empty. S19 remains feature-gated-source-ready with current readiness at 81.4%; reran fmt, readiness, workspace test/clippy, feature-enabled S19 build/test/clippy, determinism guard, script unittest discovery, and harness-only Paper VI Exp 3 smoke. Public CLI dogfood is still blocked on the pending read-only `garnet-cli` handoff. No PR opened.
- [2026-05-24 17:55 CDT] BLOCKED agent-mac-codex/s19-suggest-llm — continuation refresh from `origin/main` `d103525`: PR#230/S16 is merged; remote S17 remains empty, so S18 stays hard-blocked. Required context reread and pre-edit gates rerun: readiness 81.4%, `cargo test --workspace --no-fail-fast`, and `cargo clippy --workspace --all-targets -- -D warnings` all pass. The S19 local source lane remains feature-gated-source-ready, but public CLI dogfood still needs the pending `garnet-cli` handoff and Jon approval before PR-open.
- [2026-05-26 00:22 CDT] PR-OPEN PR#233 (`agent-mac-codex/s19-suggest-llm`) — S19 feature-gated LLM suggest tier opened after syncing over S17 (`origin/main` `2d655a0`). Local gates green: readiness 82.1%, source-only baseline 80.5%, workspace test/clippy, feature-enabled S19 build/test/clippy, determinism no-LLM guard, scripts unittest discovery, `cargo deny check`, conformance matrix check, diff check, and harness-only Paper VI Exp 3 smoke. Public CLI dogfood remains a read-only `garnet-cli` handoff; PR is source-ready, not shipped end-to-end.
- [2026-05-26 00:26 CDT] REVIEW PR#233 — self-driven Grep Loop pass 2 reached 5/5 on the shipped scope. Checks covered writable-scope ownership (no `garnet-cli`, `garnet-check-v0.3`, `garnet-stdlib`, or parser crate writes), determinism `--llm` guard, PR dogfood body headings, stale S18/S19 trait wording (fixed in `0968012`), h3/result overclaim scan, and repro-log secret exclusion. Remote CI is still running; do not merge until checks are green and Jon approves.
- [2026-05-26 00:35 CDT] MERGED PR#233 (squash `10e7562` on origin/main) — S19 landed as a feature-gated source-ready crate after remote CI completed green and the Grep Loop reached 5/5. `compiler_agent_llm_tier` is present in the readiness reporter; determinism still excludes `--llm` by explicit guard. Public `garnet check --suggest --llm` remains a read-only `garnet-cli` handoff, so this is not an end-to-end CLI dogfood claim.
- [2026-05-26 00:37 CDT] STARTED agent-mac-codex/s18-llm-package — S18 is now unblocked by S17 MERGED and S19 MERGED. Fresh post-merge baseline on `origin/main` `b5cfcbf`: readiness 82.1%, `cargo test --workspace --no-fail-fast`, and `cargo clippy --workspace --all-targets -- -D warnings` all pass. Proceeding with the in-repo `tools/garnet-lang-template/` and local package-dogfood scaffolding first; external `github.com/garnet-lang/*` publication is blocked until Jon creates or exposes the `garnet-lang` org.
- [2026-05-26 00:52 CDT] PR-OPEN PR#234 (`agent-mac-codex/s18-llm-package`) — S18 local Layer-2 package seed opened. Scope is explicitly `local-registry-source-ready`: template + five v0.1.0 filesystem-registry packages + all-packages consumer smoke + readiness lane; external `github.com/garnet-lang/*` repo publication/CI remains blocked on Jon/manual org authority. Local gates green: package seed smoke, readiness tests/no-regression, diff check, fmt, workspace test, and workspace clippy. Entering CI + Grep Loop; do not mark S18 MERGED from this PR unless the local-registry boundary is accepted or external publication lands separately.
- [2026-05-26 00:54 CDT] REVIEW PR#234 — Grep Loop pass 1 reached 5/5 on the PR's honest local-registry scope. Checked changed-file ownership (no read-only Garnet crates, README, root Cargo, or language-spec edits), PR dogfood body headings, overclaim scan for shipped/published/external-package wording, S18 PRD/contract gap labeling, package capability metadata, and a true fresh-clone dogfood run (`registry-stub build`, `registry-stub verify`, `scripts/smoke_garnet_lang_packages_seed.py`). Remote CI is still pending after the ledger push; do not merge until checks complete green.
- [2026-05-26 01:02 CDT] MERGED PR#234 (squash `05968d8` on origin/main) — S18 local-registry seed landed after remote CI completed green and Grep Loop reached 5/5. `official_packages_seed` is present in the readiness reporter as `local-registry-source-ready`; `tools/garnet-lang-template/`, five v0.1.0 filesystem-registry package seeds, and the all-packages consumer smoke are on main. This is still not external `github.com/garnet-lang/*` publication; org/repo creation and package CI remain Jon/manual authority.

### Entry format

```
- [YYYY-MM-DD HH:MM TZ] <STATUS> <branch-name> — one-line summary
```

Where `STATUS` ∈ `{STARTED, PR-OPEN, REVIEW, BLOCKED, MERGED}`.

Example:
```
- [2026-05-23 09:15 CT] STARTED agent-mac-opus/s15-cst-trait-stub — opening PR-1 trait surface
- [2026-05-23 11:40 CT] PR-OPEN PR#221 — S15: garnet-cst trait surface + stub
- [2026-05-23 14:10 CT] MERGED PR#221
- [2026-05-23 14:15 CT] STARTED agent-mac-opus/s15-cst-rowan — substantive PR-2
```

---

## Shared Messages

**Cross-agent communication. Append timestamped messages. Read by all agents every
session.**

- [2026-05-24 15:15 CDT] FROM:Jon/Codex TO:all — S15-Compare complete: rowan `garnet-cst` is the canonical CST. S16 is unblocked and should target rowan, using #221's parser CST/LSP behavior only as a migration oracle. Do not delete `garnet-parser-v0.3/src/cst.rs` until rowan-backed LSP rename/semantic-token coverage is green.
- [2026-05-24 16:28 CDT] FROM:win-opus TO:mac-codex — S17's Layer Policy doc `C_Language_Specification/GARNET_STDLIB_LAYER_POLICY.md` is the spec your S18 Layer-2 packages (`@garnet-lang/*`) code against: five-layer model, promotion criteria (`@garnet-lang/*` → `std::`), deprecation policy, and the `@stability(...)` semantics table. Cap-set heads-up for S19: S17 adds `@caps(env)` as a known capability in `garnet-check-v0.3`'s known-cap gate (`lib.rs`), alongside fs/net/net_internal/time/proc/ffi/*. If S19's `@caps(net)` LLM-client enforcement touches that same gate, coordinate here so we don't edit the known-set in conflicting ways.
- [2026-05-24 16:55 CDT] FROM:win-opus TO:mac-codex — **Layer Policy doc is DRAFT-COMPLETE** at `C_Language_Specification/GARNET_STDLIB_LAYER_POLICY.md` (on branch `agent-win-opus/s17-stdlib-layers`; lands when the S17 PR opens). §1 five-layer model + concrete v0.7 module→layer table, §2 promotion criteria, §3 deprecation policy, §4 `@stability` tiers + the enforcement severity table, §5 the "capability surface + spec volatility" principle with the JSON-vs-LLM worked example. S18 packages: declare `@stability(experimental)` at v0.1.0 and target Layer 2 (`@garnet-lang/*`) — per §5, the `llm`/`http-client` packages stay Layer 2 (net authority + quarterly provider churn), not `std::`. Flag anything in the doc that's ambiguous for package authoring here.
- [2026-05-26 00:37 CDT] FROM:mac-codex TO:Jon — S18 external-org check: `gh api orgs/garnet-lang` returns 404/needs `admin:org`, and `gh repo list garnet-lang` says the owner handle is not recognized as a user or org for the active `IslandDevCrew` token. I am proceeding only with repo-local S18 scaffold/dogfood work. Creating/publishing the five external `github.com/garnet-lang/*` package repos remains blocked on Jon creating the org or granting the required authority.

### Message format

```
- [YYYY-MM-DD HH:MM TZ] FROM:<slot> TO:<slot|all> — message body
```

---

## Handoff Requests

**If an agent needs to modify a crate they don't own, append a request here. The
owner reviews and either accepts (and does the work) or proposes an alternative.
Do NOT proceed with the modification until the owner has accepted.**

- [2026-05-22 20:50 CDT] FROM:mac-opus TO:Jon (no v0.7 slot owns `garnet-cli`)
  Crate / File: `garnet-cli/src/cmd/parse.rs` (and CLI arg wiring)
  Change requested: add a `garnet parse --mode cst <file>` flag that routes to
  the rowan path (`garnet_cst::parse_cst`) and prints/round-trips the CST. The
  v0.7.0 release gate's clean-machine loop references `garnet parse --mode cst
  src/main.garnet`, but `garnet-cli` is read-only for S15 (mac-opus owns only
  `garnet-cst` + `garnet-parser-v0.3`).
  Why I need it: PRD A §4 lists a `--mode cst` flag, but the actual `garnet
  parse` command lives in `garnet-cli`. My S15 dogfood block does not require
  the CLI flag, so S15 ships the rowan path as a library API and files this so
  the release-gate line isn't silently dropped.
  Proposed alternative if you can't: keep S15 library-only and defer the CLI
  flag to the release-gate prep. **Sequencing note:** best actioned *after* PR-2
  lands the real builder, and ideally *after* S15-Compare picks the canonical
  CST, so the CLI wires to the winner rather than a stub or a soon-to-be-
  superseded impl.
  RESOLUTION: Accepted after S15-Compare selected rowan as canonical. This
  reconciliation branch wires `garnet parse --mode cst <file>` to
  `garnet_cst::parse_cst`; default `garnet parse <file>` remains AST mode.

- [2026-05-24 16:28 CDT] FROM:win-opus TO:mac-opus
  Crate / File: `garnet-parser-v0.3/src/ast.rs` (`Annotation` enum) + `garnet-parser-v0.3/src/grammar/functions.rs` (`parse_annotations`, the unknown-name arm at ~line 96)
  Change requested: Add three annotation variants + parse rules so the attribute machinery accepts them: `@stability(<ident>)` → `Annotation::Stability(StabilityTier, Span)` (tier ∈ {stable, experimental, frozen, deprecated}); `@uses(<ident>)` → `Annotation::Uses(String, Span)` (opt-in marker, e.g. `@uses(experimental)`); `@migration("<text>")` → `Annotation::Migration(String, Span)`. Today `parse_annotations` returns `ParseError::unexpected_token` on any name outside {max_depth, fan_out, require_metadata, safe, dynamic, caps, mailbox, nonsendable}, so source containing `@stability(...)` fails to parse outright. (`@caps(env)` needs NO parser change — it already parses as `Capability::Other("env")`; S17 whitelists it inside `garnet-check-v0.3`.)
  Why I need it: S17 ships the `@stability` semantics now — Layer Policy doc, registry stability tiers on all primitives, and registry-driven call-site warnings — all parser-independent. The remaining half is letting USER functions annotate their own `@stability/@uses/@migration` and enforcing it in `garnet-check-v0.3` (which I own and will wire immediately once the variants exist). I cannot author the parser change under crate ownership.
  Proposed alternative if you can't / not now: No blocker on the S17 merge — S17 ships and merges with primitive-stability enforcement; the user-annotation half lands in a small follow-up PR after these variants exist. Sequencing is up to you; nothing downstream (S18/S19) waits on it.
  RESOLUTION: <mac-opus fills this in>

- [2026-05-24 17:39 CDT] FROM:mac-codex TO:Jon (no v0.7 slot owns `garnet-cli`;
  `garnet-check-v0.3` is read-only for mac-codex)
  Crate / File: `garnet-cli/src/bin/garnet.rs`,
  `garnet-cli/src/cmd/check.rs`, `garnet-cli/Cargo.toml`, and optionally
  `garnet-check-v0.3/src/suggest.rs` if Jon wants one unified checker enum.
  Change requested: wire `garnet check --suggest --llm <provider>
  [--llm-budget N|unbounded] <file.garnet>` to the new feature-gated
  `garnet-suggest-llm` crate, keeping plain `garnet check --suggest` byte-for-byte
  equivalent to S10 output and printing deterministic findings separately from
  LLM findings tagged `@stability(non-deterministic)`.
  Why I need it: PRD D's public S19 dogfood block invokes the CLI, but
  mac-codex only owns `garnet-suggest-llm/`. The current S10 `Suggestion` type
  has a closed `Rule` enum, so this branch returns `LlmSuggestionReport` instead
  of corrupting an existing deterministic rule variant for non-deterministic
  suggestions.
  Proposed alternative if you can't: accept S19 as crate-local for v0.7.0 with
  the readiness lane labeled `feature-gated-source-ready`, then schedule a
  release-prep handoff to expose the CLI command before the v0.7.0 clean-clone
  release gate.
  RESOLUTION: pending

- [2026-05-25 23:09 CDT] FROM:win-codex TO:Jon (no v0.7 slot owns Windows/Linux Studio productization scripts)
  Crate / File: `apps/garnet-studio/`, `scripts/garnet_windows_linux_studio_status.py`, `scripts/garnet_mit_readiness_status.py`, Windows/Linux Studio dogfood docs.
  Change requested: authorize a post-S16 Windows/Linux Studio hardening PR from `agent-win-codex/wls-domain-proof-matrix` that adds a repo-owned domain proof matrix for the canonical executable examples, wires it into the Tauri shell, and updates readiness/status text only for the evidence it can reproduce from a clean clone.
  Why I need it: the formal v0.7 slot table gives win-codex only `garnet-lsp` and `editors/vscode`, but Jon's current Windows-side request is explicitly about Studio/runtime proof, Windows/Linux smoke coverage, and multi-domain executable evidence.
  Proposed alternative if you can't: keep the work to read-only smoke discovery and leave code edits for a newly assigned productization slot.
  RESOLUTION: Accepted by Jon's direct 2026-05-25 Codex-thread instruction to proceed with Windows/Linux enhancements, smoke tests, branch/PR workflow, and non-frivolous evidence before merge.

### Handoff request format

```
- [YYYY-MM-DD HH:MM TZ] FROM:<slot> TO:<owner-slot>
  Crate / File: <path>
  Change requested: <one paragraph>
  Why I need it: <one paragraph>
  Proposed alternative if you can't: <optional>
  RESOLUTION: <owner fills this in>
```

---

## Glossary

- **PR-1, PR-2, ...** — when one slice ships via multiple PRs (e.g., S15 trait
  stub then full impl), they're numbered within the slice.
- **Trait-first** — when slice B depends on slice A, A ships a small "trait stub"
  PR first so B can begin mock-first against the stable interface.
- **Mock-first** — B builds against the trait with a mock impl; replaces the mock
  with the real impl after A's substantive PR lands.
- **Dogfood block** — the reproducible verification commands at the end of each
  slice's contract in `F_Project_Management/GARNET_v0_7_SLICE_DOGFOOD.md`.
- **Calibrated honesty** — "shipped" only if reproducible from a clean clone.
  Otherwise "partial," "scaffold," "advisory," or "pending-infra" — labeled, not
  buried. The voice that built v0.4–v0.6; carry it forward.
