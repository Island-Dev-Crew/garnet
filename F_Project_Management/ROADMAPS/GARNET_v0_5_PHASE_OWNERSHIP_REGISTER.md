# Garnet v0.5 Phase Ownership Register

Date: 2026-05-09

| Phase | Owner surface | Status | Main risk | Verification command | Handoff target |
|---|---|---|---|---|---|
| 1 Parser parity | `garnet-parser-v0.3`, `garnet-cli/tests/conformance_skeleton.rs` | started | parser accepts syntax the runtime cannot execute | `cargo test -p garnet-parser --test parse_v1_parser_parity` | conformance matrix parser-stage rows |
| 2 Managed runtime | `garnet-interp-v0.3` | not started | block/dynamic/protocol semantics become inconsistent | `cargo test -p garnet-cli --test conformance_skeleton deferred_blocks_and_yield -- --ignored` | runtime semantics addendum |
| 3 Actors + Sendable | `garnet-actor-runtime`, checker, interpreter bridge, CLI templates | Phase 3D active | full async actor-runtime OS-thread bridge remains detached from managed `Value` | `cargo test -p garnet-cli --test cli_smoke new_agent_orchestrator_template_runs_and_tests` plus actor conformance gates | async runtime bridge handoff |
| 4 Safe-mode ownership | `garnet-check-v0.3/src/{borrow,lib,match_coverage}.rs`, `garnet-parser-v0.3`, `garnet-interp-v0.3`, `garnet-cli/tests/conformance_skeleton.rs` | Phase 4AN active | full CFG NLL/dynamic-place B1-B5 plus generic/trait impl dispatch still pending; same-call double-own drop hazards, direct-returning branch liveness, direct `return` block termination, direct-returning loop-body liveness, scoped `for` loop variables, scoped `match` pattern bindings, match guard move merging, match-arm block statement preservation, finite-domain safe-mode match exhaustiveness/reachability, finite nested-constructor payload coverage, scoped named/glob/module-qualified imported enum alias coverage, immutable local boolean/enum initializer inference, direct mutable-local finite assignment inference, conservative `if`/`elsif`/`else` assignment joins for match-domain flow including nested all-path `if` branch assignments inside branch bodies, non-finite and compound mutable assignment invalidation, loop-body assignment invalidation with ordered shadowing checks, `try`/`rescue`/`ensure` assignment invalidation, uninvoked closure definition boundaries, immediate closure-literal invocation invalidation, direct local closure-literal binding call invalidation, branch-joined local closure-literal binding call invalidation, branch-rebound local closure-literal binding call invalidation, local closure-alias binding call invalidation, branch-joined local closure-alias binding call invalidation, direct branch-selected closure expression call invalidation, literal `if true` guard coverage, literal `if false` guard reachability, immutable local, same-module top-level, scoped named/glob imported top-level, and path-qualified top-level boolean guard-constant, alias, and boolean-const-expression coverage/non-coverage, including short-circuit, equality/inequality, and direct boolean guard-expression cases with parameter shadowing, and open-domain duplicate literal/catch-all reachability are now active evidence | `cargo test -p garnet-cli --test conformance_skeleton partial_borrow_rule_suite`; `cargo test -p garnet-cli --test conformance_skeleton deferred_full_borrow_rule_suite`; `cargo test -p garnet-cli --test conformance_skeleton deferred_match_exhaustiveness_and_reachability`; `cargo test -p garnet-cli --test conformance_skeleton deferred_nll_lifetime_inference`; `cargo test -p garnet-check --test borrow`; `cargo test -p garnet-check --test match_coverage`; `cargo test -p garnet-check --test extended return_ref` | full borrow/NLL checker handoff |
| 5 Traits/generics | `garnet-check-v0.3`, `garnet-interp-v0.3`, future lowering | Phase 5C active | zero-cost claims outrun backend evidence; specialization and imported-package coherence remain pending | `cargo test -p garnet-check --test coherence`; `cargo test -p garnet-cli --test conformance_skeleton deferred_trait_coherence`; `cargo test -p garnet-cli --test conformance_skeleton generic_instantiation_runs_without_monomorphization_claims` | trait/generic implementation memo |
| 6 Memory Core | `garnet-memory-v0.3` | Phase 6P active | production ARC/cycle work is algorithmically subtle; current evidence is a bounded allocator-owned root-buffer trial-deletion reference model plus kind-aware allocator stats, policy-configured lazy episodic/semantic eviction, cycle-aware store-root retain/release lifecycles, fenced episodic text snapshots, guarded append-style text log commits, a fixed typed `.garnet-cache/episodic/episodes.mnemos` backend with path/size/permission/lockfile guardrails, Unix directory-sync durability after accepted text commit renames, and source-tree rejection for copied typed cache files; cryptographic typed-cache MACs remain pending | `cargo test -p garnet-memory --test cycle`; `cargo test -p garnet-memory --test properties`; `cargo test -p garnet-memory --test persistence`; `cargo test -p garnet-memory copied_cache_backend_from_other_project_is_rejected_before_mutation`; `cargo test -p garnet-memory prepared_cache_commit_syncs_validated_directory_after_rename`; `cargo test -p garnet-cli --test conformance_skeleton deferred_arc_cycle_detection` | Memory Core implementation addendum |
| 6F-6I Cache privacy/security | `garnet-cli/src/{cache,machine_key,cmd,provenance}` | Phase 6I active | cache logs must not become a privacy leak or a strategy-poisoning channel; foreign/copy-replayed cache records should be ignored and signaled; copied strategy rows should be provenance-verified in the current source tree before CLI notes; append stress should remain parseable | `cargo test -p garnet-cli --test cache_episodes`; `cargo test -p garnet-cli --test cache_hmac`; `cargo test -p garnet-cli --test provenance`; `cargo test -p garnet-cli cache` | cache/security implementation addendum |
| 7 Proof/release/empirics | docs, release workflows, future proof repos | scaffold only | formal and empirical work cannot be honestly completed in one agent run | dogfood-readiness Part 1 after every phase | MIT/research handoff bundle |
