# L1 re-acceptance redesign — DECISION POINTS ruling record (2026-09-01)

- Ruling authority: Jon Isaac, merge-authority seat.
- Channel: directing chat, 2026-09-01 — "recommendations accepted", ruled
  against the reviewing seat's walked recommendations for all fifteen
  DECISION POINTS of the landed brief
  `F_Project_Management/W_TRUST/REACCEPTANCE_REDESIGN_BRIEF_v2.md`
  (landed by #530 at `cd4d3618…`; reviewed head `a47af844…`).
- Transporting seat: Claude Fable 5 (records lane). This record transports
  the ruling verbatim in disposition; Jon's merge of this record is the
  transcription check. Chat transport is lossy (U-71): where this record
  and Jon's intent diverge, Jon's correction supersedes by a follow-up
  records commit, never by editing this file.

## Dispositions

| DP | Ruling |
|---|---|
| 1 | **ADOPTED** — two-pair model: `native_accepted_pair` immutable; `successor_observed_pair` non-authoritative accounting; only a bounded R3 event advances the accepted pair. |
| 2 | **ADOPTED** — succession certificate, distinct suffix, append-only registry, unique-tip rule, derived B/Q/M landing model, terminal `garnet.wv_acceptance_effectiveness/v1` transcript with the no-receipt-for-the-receipt termination rule, and full rolling-review trigger/digest coverage become contract law, with the stated capture-time residual trust. |
| 3 | **ADOPTED** — #528 receives a one-time migration certificate as the closing act of the L1 implementation arc; the entry ceremony serializes against main and restarts on any intervening movement. |
| 4 | **ADOPTED** — the same-run Re-run-all-jobs cure with the canonical attempt-1 eligibility artifact and an exact `actions: read` CI delta. Carrier: a distinct authenticated Actions-write identity, to be minted in the fleet identity rollout; until it exists, `r2_role_separation_v1` remains OPEN-UNTIL-IMPLEMENTED and blocks R2 activation, not R2 construction. |
| 5 | **ACCEPTED AS RESIDUAL** — the readback-to-merge-click race is irreducible under the zero-approval ruleset and is documented, never concealed; the final premerge readback is performed by both the reporter (emission) and Jon (reading before the click). |
| 6 | **ADOPTED** — the three R3 event families and their impact-proof matrices; any changed input reaching native behavior without a closed class proof routes to native Windows replay. |
| 7 | **ADOPTED WITH PRECONDITION** — Class A may be ratified only after the standing `arrayref@0.3.9` deny-row is removed in a separate ceremonied PR (the yank is reversed; the exception is inert; removal is STRENGTHENING and exercises the class expiry shape). That removal PR is authorized by this ruling. |
| 8 | **ADOPTED** — Class B remains a closed set with zero autonomous future events; expanding the lossy live top-level projection (U-76) or admitting a new typed leaf requires a prior Jon ruling and a separate contract act; the U-76 cure folds into that same future act. |
| 9 | **ADOPTED** — the predecessor-base Class C rewrite catalog with `C-RW-0001` as its first construction-proof entry; every catalog miss routes to native replay. |
| 10 | **ADOPTED** — cross-family symmetry as exercised this arc (Codex implements ↔ Claude reviews, reversible); any BOUNDED WEAKENING additionally requires a second reviewing family. |
| 11 | **ADOPTED** — the exhaustive predecessor record-consumer inventory and all four named conservation predicates (`r1_review_scope_exact_v1`, `r2_role_separation_v1`, `r1_reporter_constant_projection_v1`, `r1_strict_equal_blob_identity_v1`) are the required activation shape; OPEN-UNTIL-IMPLEMENTED is ineligible for activation; prose is not an alternative. |
| 12 | **ADOPTED WITH ADDITION** — the integrity-over-availability unique-tip law stands; a registry-fork recovery runbook must be written as a records file BEFORE first activation, so fork recovery is never improvised. |
| 13 | **DISCHARGED BY EVENTS** — the sweep allocations became authoritative at #529/#532; the U-39 cure landed separately at #533 exactly as required. |
| 14 | **AUTHORIZATION GRANTED** — this ruling commissions the L1 implementation arc (contract-law texts, CI wiring, producers/registries/inventory/predicates/fixtures, gate wiring, migration certificate), each act under full ceremony; the brief itself remains non-operative. |
| 15 | **AFFIRMED** — merge, acceptance, launch state, FIRE/HOLD, and release actions remain Jon-only. |

## Operative consequences

1. The L1 implementation arc is authorized, sequenced as: contract-law
   texts → CI wiring (rides alone; gate-modifying) → producers, registries,
   consumer inventory, executable predicates, adversarial fixtures, and the
   DP12 recovery runbook → gate-side wiring (rides alone; gate-modifying)
   → the #528 migration certificate (serialized against main).
2. The Class A deny-row removal PR is authorized (DP7 precondition).
3. The fleet identity rollout (mapped seat accounts for the NUC and Air
   seats plus a distinct carrier identity) is the standing prerequisite for
   `r2_role_separation_v1` and cures the registered seat-identity/record-
   schema incompatibility.
4. L9 (31→32 activation) remains gated on Jon's separate activation-
   evidence ruling; the policy-layer observation (`candidate_policy_ok`
   true on both #534 firings) stands recorded.
