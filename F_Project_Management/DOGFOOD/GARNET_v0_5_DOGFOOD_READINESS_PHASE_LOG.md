# Garnet v0.5 Dogfood Readiness Phase Log

Date: 2026-05-09

This log records whether the language-completion roadmap survives actual use.
It is not a replacement for dogfood-readiness reports; it is the phase ledger
that each report should consume.

| Phase | Dogfood gate | Current result | Evidence |
|---|---|---|---|
| 1 Parser parity | New Mini-Spec syntax parses without breaking MVPs | parser-stage `do...end` support landed and remains covered while Phase 2A builds runtime semantics on top | `cargo test -p garnet-parser --test parse_v1_parser_parity`; `cargo test -p garnet-parser`; `cargo test -p garnet-cli --test conformance_skeleton`; `cargo test -p garnet-cli --test dogfood_readiness_examples`; `cargo test -p garnet-cli --test examples` |
| 2 Managed runtime | Parser-stage features run in managed mode | Phase 2H active: syntactic `do...end` block invocation, `yield`, `next`, per-instance `@dynamic` method tables, static inherent impl fallback, `method_missing`, protocol-typed managed parameter checks, protocol method signature checks, runtime `as Protocol` casts, generic protocol substitution, core built-in typed method signatures, and `@dynamic impl` dispatch tables are active. Ordinary trait coherence and broader generic impl resolution remain deferred. | `cargo test -p garnet-cli --test conformance_skeleton`; `cargo test -p garnet-cli --test conformance_phase_gates`; `cargo test -p garnet-parser --test parse_v1_parser_parity` |
| 3 Actors + Sendable | `agent-orchestrator` uses actor syntax frictionlessly and nonsendable actor payloads are rejected | Phase 3D active: `@nonsendable` actor protocol/handler payload rejection, synchronous managed `spawn Actor.handler(args)` dispatch, `spawn Actor` managed addresses, bounded source mailboxes via `Actor.spawn(capacity)`, and a generated `agent-orchestrator` project that runs actor syntax plus actor-local episodic/semantic/procedural memory. Full async actor-runtime OS-thread bridge remains pending. | `cargo test -p garnet-check --test extended actor_protocol_rejects_nonsendable_payload_type`; `cargo test -p garnet-cli --test conformance_skeleton actor_sendable_rejects_nonsendable_protocol_payloads`; `cargo test -p garnet-cli --test examples multi_agent_builder_runs_with_managed_actor_bridge`; `cargo test -p garnet-interp c5_actor_handler_dispatches_via_spawn_bridge`; `cargo test -p garnet-interp c5_spawn_actor_returns_address_with_persistent_state`; `cargo test -p garnet-interp c5_actor_address_enforces_bounded_mailbox`; `cargo test -p garnet-interp c5_actor_address_tell_reports_full_mailbox`; `cargo test -p garnet-interp c5_actor_spawn_rejects_extra_capacity_args`; `cargo test -p garnet-parser parses_spawn_keyword_as_member_method_name`; `cargo test -p garnet-cli --test cli_smoke new_agent_orchestrator_template_runs_and_tests` |
| 4 Safe mode | unsafe ownership examples are rejected, valid examples pass | Phase 4BI active: top-level conformance now rejects direct use-after-move through `own` parameters, direct `mut`+`borrow` aliasing, `own self` method receiver use-after-move, method receiver aliasing, simple typed receiver disambiguation for same-named impl methods, simple field-place aliasing/field use-after-move, conservative index-place aliasing/index use-after-move, nested index operand misuse, overlapping `own` places in one call as a B5 drop-discipline violation, direct-returning `if`/`else` branch liveness, direct `return` block termination, direct-returning `while`/`loop`/`for` body liveness, scoped `for` loop variables that cannot erase moved outer bindings, scoped `match` pattern bindings that cannot poison same-named outer bindings, match guard moves that merge when the guard can fail, match-arm block statements that execute and participate in move diagnostics, finite-domain `Bool`/same-module enum match exhaustiveness, finite nested-constructor payload coverage, scoped named/glob/module-qualified imported enum alias coverage, immutable local boolean/enum initializer inference, direct mutable-local finite assignment inference, conservative `if`/`elsif`/`else` assignment joins for match-domain flow including nested all-path `if` branch assignments inside branch bodies, non-finite and compound mutable assignment invalidation, loop-body assignment invalidation with ordered shadowing checks, `try`/`rescue`/`ensure` assignment invalidation, uninvoked closure definition boundaries, immediate closure-literal invocation invalidation, direct local closure-literal binding call invalidation, branch-joined local closure-literal binding call invalidation, branch-rebound local closure-literal binding call invalidation, local closure-alias binding call invalidation, branch-joined local closure-alias binding call invalidation, direct branch-selected closure expression call invalidation, literal `if true` match-guard coverage, literal `if false` guard reachability/non-coverage, immutable local, same-module top-level, scoped named/glob imported top-level, and path-qualified top-level boolean guard-constant, alias, and boolean-const-expression coverage/non-coverage, including short-circuit, equality/inequality, direct boolean guard-expression cases, and checked integer arithmetic/equality/inequality/relational const guard cases in alias, direct match guard, same-module bare-name, and scoped named/glob imported integer const identifier forms with parameter shadowing, static boolean relational const guards, static nil/symbol/string equality/inequality const guards including static interpolated strings, static string relational const guards, static nil relational const guards, mixed known-literal equality/inequality facts, plus finite float equality/inequality, int-float equality, finite float/int-float relational facts, and finite float/int-float arithmetic facts, immutable local boolean/integer const-expression guard aliases with mutable-local invalidation, path-qualified top-level const references inside those local aliases, duplicate covered-arm reachability, open-domain duplicate literal reachability, open-domain arms-after-catch-all reachability, and ambiguous reference-return lifetime elision while preserving managed ARC semantics. Full CFG NLL/lifetime inference, cross-file/package imports, non-finite floats, call-backed/dynamic interpolated strings, broader non-boolean non-string non-numeric comparison, broader float edge-case reasoning, function-call, and broader const expression evaluation beyond immutable local aliases and path-qualified const references, recursive/open payload reasoning, broader expression/type inference, loop fixed-point domain inference, broader mutable/escaped/general higher-order closure invocation/call-effect analysis, open-domain exhaustiveness/range reasoning, broader non-literal guard reasoning, dynamic place tracking, generic/trait impl dispatch, broader drop elaboration, general loop fixed-point analysis, and two-phase borrows remain pending. | `cargo test -p garnet-cli --test conformance_skeleton partial_borrow_rule_suite`; `cargo test -p garnet-cli --test conformance_skeleton deferred_full_borrow_rule_suite`; `cargo test -p garnet-cli --test conformance_skeleton deferred_match_exhaustiveness_and_reachability`; `cargo test -p garnet-cli --test conformance_skeleton deferred_nll_lifetime_inference`; `cargo test -p garnet-check --test borrow`; `cargo test -p garnet-check --test match_coverage`; `cargo test -p garnet-check --test extended return_ref` |
| 5 Traits/generics | trait/generic examples check and run honestly | Phase 5C active: exact duplicate trait impls, orphan impls, simple generic blanket-vs-concrete overlaps, renamed generic blanket overlaps, and qualified external type short-name collisions are rejected while local-trait, local-type, and qualified local-module impls remain accepted; generic struct construction, generic impl method dispatch, and generic function calls run through the managed interpreter. Specialization, imported-package coherence, and native monomorphization remain pending. | `cargo test -p garnet-check --test coherence`; `cargo test -p garnet-cli --test conformance_skeleton deferred_trait_coherence`; `cargo test -p garnet-cli --test conformance_skeleton generic_instantiation_runs_without_monomorphization_claims` |
| 6 Memory Core | cycle, kind-aware, and fenced persistence memory fixtures pass | Phase 6Q + Phase 6R + Phase 6S + Phase 6U active: bounded root-buffer trial-deletion, finalization-order, safe-mode exclusion, allocator-owned root/edge decrement fixtures, kind-aware store allocator stats, policy-configured lazy episodic/semantic eviction, cycle-aware store-root retain/release lifecycles, concrete `CycleAwareKindAllocator` root-release finalization and safe-affine exclusion evidence (Phase 6Q), allocator-facing buffered edge-removal collection evidence with threshold-crossing trial candidates / finalization-order / collected / root-stats reporting across all four memory kinds and safe-affine exclusion preserved (Phase 6R), allocator-owned finalizer-log evidence for plain release/collect/remove-edge calls without per-call callbacks (Phase 6S), episodic text snapshot save/load with malformed-file non-mutation, guarded append-style text log commits, fixed `.garnet-cache/episodic/episodes.mnemos` typed backend guardrails, Unix directory-sync durability after atomic text commit renames, typed-cache source-tree replay rejection, and opt-in BLAKE3-keyed typed-cache record MAC append/load APIs that reject tampered payloads and foreign keys before live mutation (Phase 6U); production allocator-integrated ARC, user-payload finalizer invocation, and broad pluggable signed persistence backends remain pending | `cargo test -p garnet-memory --test cycle`; `cargo test -p garnet-memory --test properties`; `cargo test -p garnet-memory --test persistence`; `cargo test -p garnet-memory copied_cache_backend_from_other_project_is_rejected_before_mutation`; `cargo test -p garnet-memory prepared_cache_commit_syncs_validated_directory_after_rename`; `cargo test -p garnet-cli --test conformance_skeleton deferred_arc_cycle_detection` |
| 6F Cache privacy/security | compiler-as-agent cache does not leak absolute user/temp/CI paths and HMAC/provenance gates stay green | Phase 6F active: cache episode records now store project-local absolute paths as stable relative labels and redact external absolute paths to `<external>/<file>` while preserving existing CacheHMAC and ProvenanceStrategy behavior | `cargo test -p garnet-cli --test cache_episodes`; `cargo test -p garnet-cli --test cache_hmac`; `cargo test -p garnet-cli --test provenance`; `cargo test -p garnet-cli cache` |
| 6G Cache replay stress | foreign machine-key and copied-cache episodes cannot influence prior-failure advice without a warning | Phase 6G active: same-cache foreign machine-key episodes and copied `.garnet-cache` replay episodes are skipped, counted, and surfaced as untrusted instead of being used as local prior-failure evidence | `cargo test -p garnet-cli --test cache_episodes foreign_machine_episode_in_same_cache_is_ignored_and_warned`; `cargo test -p garnet-cli --test cache_episodes copied_foreign_cache_replay_is_ignored_and_warned` |
| 6H Strategy replay/concurrency | copied strategy DB rows cannot influence CLI strategy notes unless provenance re-verifies, and bounded concurrent episode appends stay intact | Phase 6H active: copied same-machine `strategies.db` rows with missing local justifying episodes are quarantined before strategy notes are printed, and 8-writer bounded episode append stress preserves all verified records | `cargo test -p garnet-cli --test cache_episodes copied_strategy_db_without_local_justifications_is_quarantined_and_warned`; `cargo test -p garnet-cli --test cache_episodes concurrent_episode_appends_preserve_all_verified_records` |
| 6I Source-tree binding + soak | same-machine cache copies from another project root cannot influence prior-failure or strategy advice, and larger bounded append stress stays parseable | Phase 6I active: episodes carry a keyed source-tree binding, copied same-machine episode logs are skipped and warned as untrusted, copied same-machine strategy rows with replayed justifications are quarantined, and 16-writer/1920-record append soak preserves valid NDJSON plus all verified records | `cargo test -p garnet-cli --test cache_episodes copied_same_machine_cache_from_other_source_tree_is_ignored_and_warned`; `cargo test -p garnet-cli --test cache_episodes copied_same_machine_strategy_with_replayed_episodes_is_quarantined`; `cargo test -p garnet-cli --test cache_episodes episode_append_soak_preserves_ndjson_and_all_bound_records` |
| 6P Typed cache source-tree binding | typed Mnemos cache files copied from another project root cannot replace live episodic memory | Phase 6P active: `episodes.mnemos` files carry a source-tree binding derived from the canonical project root, appends validate existing bindings before rewrite, loads reject mismatched copied caches before live-store replacement, and same-project cache round-trips remain green. Cryptographic typed-cache MACs remain future work. | `cargo test -p garnet-memory copied_cache_backend_from_other_project_is_rejected_before_mutation`; `cargo test -p garnet-memory --test persistence episodic_cache_append_and_load_round_trips_under_default_cache_dir`; `cargo test -p garnet-memory --test persistence` |
| 7 Proof/release/empirics | release/proof/benchmark claims are separated and falsifiable | release lane active: org `v0.4.2` release is published and release-backed installer smoke passed with SHA-256 verification and macOS tarball fallback; proof and benchmark lanes remain scaffold-only | `gh release view v0.4.2 --repo Island-Dev-Crew/garnet`; `./scripts/verify_org_release_smoke.sh`; runbooks and research handoffs |
| Security/trust boundary | FS/net/db/converter/release/cache authority surfaces are inventoried and probed | in progress; cache path privacy, replay-warning, source-tree binding, strategy-provenance, bounded soak gates, Phase 6N typed episodic backend path/permission/size/lockfile guardrails, Phase 6O validated-directory sync evidence, and Phase 6P typed-cache replay rejection active | `GARNET_SECURITY_DOGFOOD_RUBRIC.md`; `cargo audit`; `cargo deny --all-features check`; `cargo test -p garnet-memory --test persistence`; `cargo test -p garnet-cli --test cache_episodes`; source trust-boundary scan |
| Dogfood PR evidence gate | Readiness-sensitive PRs carry reproducible current-truth, local/remote verification, Desktop bundle, and deferred-boundary evidence | Phase 6S process gate active: a `no-mistakes`-inspired PR body checker now skips unrelated diffs, gates readiness-sensitive paths, requires the `## Dogfood Readiness` evidence section, and rejects unqualified production ARC completion claims before merge review | `python3 scripts/test_check_dogfood_pr_body.py`; `python3 scripts/check_dogfood_pr_body.py --base <base> --head <head>`; `.github/workflows/dogfood-readiness.yml` |
| Dogfood readiness skill + slice status | Future readiness slices expose their tracked-plan completion percentage and preserve local-gate evidence before remote merge | Phase 6T process gate active: `GARNET_DOGFOOD_READINESS_SKILL.md` makes the dogfood workflow model-agnostic, and `scripts/garnet_readiness_status.py` reports implementation-plan checkbox completion plus open slices for PR bodies, Desktop bundles, and future dashboards | `python3 scripts/test_garnet_readiness_status.py`; `python3 scripts/garnet_readiness_status.py`; `.github/workflows/dogfood-readiness.yml` |
| Converter adoption truth | Users and agents can distinguish active converter support from advisory planning, native-boundary, backend-lowering, and LLM-assist adoption claims | Process/product gate active: `scripts/garnet_converter_status.py` reports Rust/Ruby/Python/Go as the only active deterministic converter lanes, lists JavaScript/TypeScript/Swift/Java/C/C++/C#/Perl/Kotlin/Shell/SQL/Other as advisory planning lanes, labels C/C++/Objective-C/Assembly/CUDA/platform-specific code as native-boundary recommended, and keeps LLM assist proposed, advisory-only, sandboxed, lineage-preserving, `garnet check`-gated, and dogfood-gated. The reporter now exposes a planned Garnet-aware assist contract with source classifier, risk inventory, context pack, advisory plan, review handoff, human-approved candidate, safe-mode/memory/CapCaps/actor analysis targets, provider/model optionality, lineage, sandbox, dogfood-bundle, and human-audit gates. The agentic matrix probes this status so repo/site claims cannot silently drift. | `python3 scripts/test_garnet_converter_status.py`; `python3 scripts/garnet_converter_status.py`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict` |
| Garnet-aware assist context pack | Future provider-backed converter assist has a deterministic current-truth context and prompt contract before any model or network lane is introduced | Process/product gate active: `scripts/garnet_assist_context_pack.py` emits JSON/Markdown plus a manifest for the current truth, public README, Mini-Spec, conformance matrix, and dogfood ledger. Phase 6AE adds a provider-neutral prompt pack and `garnet-assist-prompt-pack.md` artifact so a future model/agent handoff can consume the same gates without enabling provider-backed conversion. It preserves the planned assist contract, analysis targets, required gates, forbidden claims, and prompt instructions while explicitly reporting that provider-backed LLM conversion is not active today. The agentic matrix probes the context and prompt pack so drift in required documents, gates, prompt boundaries, or current-truth boundaries fails readiness. | `python3 scripts/test_garnet_assist_context_pack.py`; `python3 scripts/garnet_assist_context_pack.py --format json`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict` |
| Converter assist planning | Agents can produce useful per-file advisory-language migration evidence without claiming active LLM conversion or broad deterministic frontend support | Process/product gate active: `scripts/garnet_converter_assist_plan.py` reads one active or advisory source file, never executes source, and emits JSON/Markdown plus a manifest with current truth, source hash, safe-mode/memory/CapCaps/actor analysis targets, shell/process and SQL/data risk inventory, migration-risk inventory, and required gates. The agentic matrix now includes TypeScript, JavaScript, Swift, Java, C, C++, C#, Perl, Kotlin/Shell/SQL/Other status coverage, and native-boundary truth probes so web-agent, Apple-app, JVM-service, native-memory, managed-service, scripting, and data migration evidence is useful but still quarantined behind lineage, `@sandbox`, `garnet check`, dogfood bundle, and human audit. Phase 6AX keeps broad languages advisory-only or native-boundary rather than active conversion. | `python3 scripts/test_garnet_converter_assist_plan.py`; `python3 scripts/garnet_converter_assist_plan.py --language typescript --source <file>`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict` |
| Converter LLM feasibility | The converter-stage LLM question has a falsifiable readiness answer before any provider/model/network lane is enabled | Phase 6AO/6AX active: `scripts/garnet_converter_llm_feasibility.py` reports that provider-neutral advisory planning is feasible, but autonomous/provider-backed LLM conversion is not active and remains blocked on secure advisory implementation, provider/runtime boundaries, deterministic frontend gates, native-boundary review, lineage, `@sandbox`, `garnet check`, dogfood readiness, and human audit. The agentic matrix probes current truth, language coverage, blockers, and manifested JSON/Markdown output for this reporter; Phase 6AX also records the approved source-classifier to risk-inventory to context-pack to review-handoff pipeline before candidate output can be trusted. | `python3 scripts/test_garnet_converter_llm_feasibility.py`; `python3 scripts/garnet_converter_llm_feasibility.py`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict` |
| Converter advisory bundle | Future model/agent handoff can consume feasibility, current-truth context, and per-file assist-plan evidence as one manifested package without enabling conversion | Phase 6AP active: `scripts/garnet_converter_advisory_bundle.py` combines the converter LLM feasibility decision, deterministic context pack, and per-file assist plan into JSON/Markdown/request artifacts plus a verified manifest. It reports provider/model/network not required, keeps conversion inactive, omits source text by default for privacy/provider-boundary safety, requires explicit `--include-source` to embed source, and preserves lineage, `@sandbox`, `garnet check`, dogfood readiness, and human-audit gates. The agentic matrix adds four advisory-bundle probes for current truth, default source omission, explicit source inclusion, and manifest verification; current source-checkout evidence passes `88/88` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-090352`. | `python3 scripts/test_garnet_converter_advisory_bundle.py`; `python3 scripts/garnet_converter_advisory_bundle.py --language typescript --source <file> --output-dir <bundle>`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict` |
| Converter advisory review | Future model/agent handoff must pass a local review gate before advisory evidence is treated as implementation input | Phase 6AS active: `scripts/garnet_converter_advisory_review.py` reads a manifested advisory bundle, verifies `MANIFEST.sha256`, confirms default source omission, blocks source-included bundles unless explicitly approved, keeps provider-backed conversion disallowed, and emits JSON/Markdown review evidence with lineage, `@sandbox`, `garnet check`, dogfood evidence, and human-audit checklist items. The agentic matrix adds a three-probe `converter advisory review` domain for current truth, source-included blocking, and review-manifest evidence; current source-checkout evidence passes `94/94` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-100014`. The packaged app/DMG smoke passes in `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-100034` for DMG SHA-256 `793aaec45055c338fda45386006f545356b4d0a2adad79ad8e6048ef012dee36`. | `python3 scripts/test_garnet_converter_advisory_review.py`; `python3 scripts/garnet_converter_advisory_review.py --bundle-dir <bundle>`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict` |
| Converter advisory handoff | Future model/agent handoff can receive a reviewed, no-source prompt packet without enabling provider-backed conversion | Phase 6AV active: `scripts/garnet_converter_advisory_handoff.py` consumes a manifested advisory bundle and review-gate output, refuses blocked or source-included reviews, emits JSON/Markdown source-free handoff evidence, and keeps model calls plus conversion inactive. The agentic matrix adds a three-probe `converter advisory handoff` domain for current truth, source-included blocking, and manifest verification; current source-checkout evidence passes `103/103` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-103651`. The packaged app/DMG smoke passes in `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-103813` for DMG SHA-256 `396c5afc7e5409fc9977d0dcd582a363851f65f1479ceb7be8f1c838d94cb932`. | `python3 scripts/test_garnet_converter_advisory_handoff.py`; `python3 scripts/garnet_converter_advisory_handoff.py --bundle-dir <bundle> --review-dir <review> --output-dir <handoff>`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict`; `scripts/smoke_garnet_studio_dmg.sh --copy-to-desktop target/macos/GarnetStudio.dmg` |
| Converter advisory bundle UX | macOS users can create the same provider-neutral advisory package from Garnet Studio without opening Terminal or embedding source by default | Phase 6AQ/6AR active: the Studio Converter panel now exposes `Advisory Bundle` beside `Assist Plan`. The Swift runner locates `garnet_converter_advisory_bundle.py` from packaged resources, `GARNET_REPO_ROOT`, or the source checkout, writes a temporary input outside the preserved bundle, writes manifested output under `~/Desktop/dogfood/garnet-studio-advisory-bundle-<stamp>`, reports the bundle path in the console, and never passes `--include-source` from the app. The agentic matrix adds three UX probes for the Studio action, runner, and default source-omission boundary. It preserved a failed `88/91` run at `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-091859` before fixing the probe path, then passed `91/91` with `skipped=0` in Desktop bundles `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-091949` and `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-093319`. | `swift test --package-path apps/garnet-studio-macos`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench`; `apps/garnet-studio-macos/Sources/GarnetStudio/GarnetStudioApp.swift` |
| Converter advisory review UX | macOS users can run the same local review gate from Garnet Studio before any model/agent handoff | Phase 6AT active: the Studio Converter panel now exposes `Advisory Review` beside `Advisory Bundle`. The action creates a default no-source advisory bundle, runs `garnet_converter_advisory_review.py` without `--allow-source-included`, and preserves manifested review output under `~/Desktop/dogfood/garnet-studio-advisory-review-<stamp>`. The agentic matrix adds a three-probe `converter advisory review UX` domain for the app action, runner, and Desktop evidence path; current source-checkout evidence passes `97/97` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-101247`. The packaged app/DMG smoke passes in `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-101312` for DMG SHA-256 `68babfa7974a4b31173651eee27f0e5f6844637486478c42802b53bd34a80863`. | `swift test --package-path apps/garnet-studio-macos`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench`; `scripts/smoke_garnet_studio_dmg.sh --copy-to-desktop target/macos/GarnetStudio.dmg`; `apps/garnet-studio-macos/Sources/GarnetStudio/GarnetStudioApp.swift` |
| Converter advisory handoff UX | macOS users can package the reviewed no-source handoff packet from Garnet Studio before any model/agent execution | Phase 6AW active: the Studio Converter panel exposes `Advisory Handoff` beside `Advisory Review`. The action creates a default no-source advisory bundle, runs `garnet_converter_advisory_review.py` without `--allow-source-included`, then packages the reviewed context through `garnet_converter_advisory_handoff.py` without source inclusion and preserves output under `~/Desktop/dogfood/garnet-studio-advisory-handoff-<stamp>`. The agentic matrix adds a three-probe `converter advisory handoff UX` domain for the app action, runner, and Desktop evidence path; current source-checkout evidence passes `106/106` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-105145`. The packaged app/DMG smoke passes in `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-105515` for DMG SHA-256 `3eaee71e813a67c299411ba981eb6588a299e7ea8e7698317c6d18911423dee8`. This does not enable provider-backed conversion or trust candidate output before lineage, `@sandbox`, `garnet check`, dogfood evidence, and human audit. | `swift test --package-path apps/garnet-studio-macos`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench`; `scripts/smoke_garnet_studio_dmg.sh --copy-to-desktop target/macos/GarnetStudio.dmg`; `apps/garnet-studio-macos/Sources/GarnetStudio/GarnetStudioApp.swift` |
| MIT objective pulse UX | macOS users can see the broader MIT/productization completion percentage from the app without confusing it with tracked-slice completion | Phase 6AU active: the Studio Release panel exposes `Objective Pulse`, which runs `scripts/garnet_mit_readiness_status.py --format markdown` from packaged resources, `GARNET_REPO_ROOT`, or the source checkout. The action is read-only and preserves the split between `87/87` tracked slices and the broader active-partial MIT/productization objective. The agentic matrix adds a three-probe `MIT objective pulse UX` domain for the app action, runner, and truth copy; current source-checkout evidence passes `100/100` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-102355`. The packaged app/DMG smoke passes in `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-102422` for DMG SHA-256 `88c1cbe96fe3bc3fa38d44fcf574970400e7739156bb569106790d4e801a22b6`. | `swift test --package-path apps/garnet-studio-macos`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench`; `scripts/smoke_garnet_studio_dmg.sh --copy-to-desktop target/macos/GarnetStudio.dmg`; `scripts/garnet_mit_readiness_status.py --format markdown` |
| Mac continuation pulse UX | macOS users can see the current Mac-actionable, blocked, and delegated continuation split from the app and the landing page names that workflow without overclaiming deployment | Phase 6AZ/6BA active: the Studio Release panel exposes `Continuation Pulse`, which runs `scripts/garnet_mac_side_continuation_status.py --format markdown` from packaged resources, `GARNET_REPO_ROOT`, or the source checkout. Phase 6BA also exposes the same workflow in the public `docs/index.html` Studio workbench hook and makes missing `Continuation Pulse` copy a strict Pages smoke blocker. The action and site copy are read-only and keep Mac-actionable repo/productization lanes separate from Apple Developer ID notarization, Windows/Linux Studio runtime proof, provider-backed LLM conversion, and native backend lowering. App-side source-checkout evidence passes `110/110` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-164602`; packaged app/DMG smoke passes in `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-164632`; local Pages smoke passes with blockers/warnings at zero in `/Users/idc2.0/Desktop/dogfood/pages-pwa-readiness-20260516-165857`; local web/PWA smoke passes in `/Users/idc2.0/Desktop/dogfood/web-pwa-readiness-20260516-165857`. | `swift test --package-path apps/garnet-studio-macos`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench`; `./scripts/package_garnet_studio_macos.sh`; `scripts/smoke_garnet_studio_dmg.sh --copy-to-desktop target/macos/GarnetStudio.dmg`; `scripts/garnet_mac_side_continuation_status.py --format markdown`; `python3 scripts/test_smoke_garnet_pages_pwa.py`; `scripts/smoke_garnet_pages_pwa.sh --base-url http://127.0.0.1:8876 --copy-to-desktop --strict`; `scripts/smoke_garnet_web_pwa.sh --copy-to-desktop --strict` |
| MIT demo-route evidence | Reviewers can rehearse a bounded Garnet MIT walkthrough from current verified evidence without confusing it with final acceptance | Phase 6BB active: `scripts/garnet_mit_demo_route.py` emits JSON/Markdown for a seven-minute route through Objective Pulse, Garnet Studio continuation, converter advisory handoff, agentic dogfood, public web/PWA evidence, and blocked-gate closeout. The route keeps the live objective completion percentage, `87/87` tracked slices, and blocked/deferred claims separate so clean CI and Desktop-enriched local runs can both stay truthful; it explicitly forbids Developer ID notarization, Windows/Linux Studio runtime proof, provider-backed LLM conversion, native backend lowering, mobile distribution, production-ready language, and final MIT/productization acceptance claims. Source-checkout evidence passes `113/113` with `skipped=0` in `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-171902`; standalone route evidence verifies in `/Users/idc2.0/Desktop/dogfood/garnet-mit-demo-route-20260516-171836`; packaged app/DMG smoke verifies the staged route reporter in `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-172150` for DMG SHA-256 `52fec2da40597de6dbaecb4b21562cf19007e4ec584f0f8174545f9bd13fe9f3`. | `python3 scripts/test_garnet_mit_demo_route.py`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench`; `python3 scripts/test_garnet_studio_packaging_resources.py`; `./scripts/package_garnet_studio_macos.sh`; `shasum -a 256 -c MANIFEST.sha256` |
| Promo video readiness contract | The future 30-second Garnet ad lane is storyboarded, source-locked, composition-ready, locally renderable, automated-visual-QA-ready, and gated before any public video claim reaches the repo or site | Process/product gate active: `scripts/garnet_promo_video_status.py` reports the promo lane as `composition-ready` in clean source checkouts, `rendered-artifact-ready` when local Desktop MP4/WebM outputs are present, and `visual-qa-ready` when automated visual-QA evidence is present. It records the five-beat 30-second storyboard, verifies canonical logo/PWA icon assets plus the public site, Garnet Studio, dogfood matrix, and MIT readiness reporter source surfaces, verifies `docs/promo/DESIGN.md` plus the HyperFrames-compatible `docs/promo/composition.html` source registered as `garnet-promo-main`, gates `scripts/render_garnet_promo_video.mjs` as a local Chrome/CDP plus `ffmpeg` render harness, and gates `scripts/qa_garnet_promo_video.mjs` as an automated `ffprobe`/`ffmpeg` visual-QA harness. Local rendered evidence at `/Users/idc2.0/Desktop/dogfood/garnet-promo-video` verifies 30.000-second 1920x1080 H.264 MP4 and VP9 WebM artifacts at 12 fps with a manifest. Local automated visual-QA evidence at `/Users/idc2.0/Desktop/dogfood/garnet-promo-video-visual-qa` verifies metadata and representative frame extraction with a manifest. Website export, Desktop acceptance, and overclaim checks remain open before any shipped-video claim. The agentic matrix now includes a seven-probe `promo video readiness` domain including source-lock, composition-source, render-harness, visual-QA-harness, and manifest verification. | `python3 scripts/test_garnet_promo_video_status.py`; `python3 scripts/test_render_garnet_promo_video.py`; `python3 scripts/test_qa_garnet_promo_video.py`; `node scripts/render_garnet_promo_video.mjs --help`; `node scripts/qa_garnet_promo_video.mjs --help`; `python3 scripts/garnet_promo_video_status.py --output-dir <bundle>`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict` |
| Promo website-export package | Visual-QA-approved local promo media can be packaged for future site embedding without claiming the site already uses it | Phase 6AL active: `scripts/export_garnet_promo_video_site.mjs` requires passing visual-QA evidence, copies MP4/WebM/poster assets, writes `embed-snippet.html`, JSON/Markdown evidence, and a verified manifest into `/Users/idc2.0/Desktop/dogfood/garnet-promo-video-website-export`. `scripts/garnet_promo_video_status.py` can now report `website-export-ready` at 90.0% when render, visual-QA, and export evidence are all present; public-site embedding, human/aesthetic acceptance, and overclaim checks remain separate gates. The agentic matrix includes an eighth promo-video readiness probe for the website-export harness contract. | `python3 scripts/test_export_garnet_promo_video_site.py`; `node scripts/export_garnet_promo_video_site.mjs --help`; `scripts/export_garnet_promo_video_site.mjs --input-dir /Users/idc2.0/Desktop/dogfood/garnet-promo-video --qa-dir /Users/idc2.0/Desktop/dogfood/garnet-promo-video-visual-qa --output-dir /Users/idc2.0/Desktop/dogfood/garnet-promo-video-website-export`; `shasum -a 256 -c MANIFEST.sha256` |
| Promo public-site sync | Verified promo export media can be embedded in the repo docs site while final human/aesthetic acceptance remains open | Phase 6AM active: `scripts/sync_garnet_promo_video_site.mjs` requires passing website-export evidence, copies MP4/WebM/poster assets into `docs/assets/`, verifies the public-site `<video>` block and service-worker cache references, writes `promo-site-sync-data.json`, `promo-site-sync-report.md`, and `MANIFEST.sha256`, and preserves the boundary that public-site embedded is not final marketing acceptance or full MIT/productization completion. `scripts/garnet_promo_video_status.py` can now report `public-site-embedded` at 95.0% when render, visual-QA, website-export, and site-sync evidence are all present. | `python3 scripts/test_sync_garnet_promo_video_site.py`; `node scripts/sync_garnet_promo_video_site.mjs --help`; `scripts/sync_garnet_promo_video_site.mjs --export-dir /Users/idc2.0/Desktop/dogfood/garnet-promo-video-website-export --docs-dir docs --output-dir /Users/idc2.0/Desktop/dogfood/garnet-promo-video-site-sync`; `/Users/idc2.0/Desktop/dogfood/garnet-promo-video-site-sync/MANIFEST.sha256` |
| Public progress pulse | Visitors and agents can see the overall MIT/productization percentage without confusing it with tracked-slice completion | Phase 6AN/6AX active: `docs/index.html` now shows the current local `58.6%` MIT/productization objective checkpoint and `87/87 tracked slices` as a landing-page pulse, while `docs/status.html` carries detailed open gates for notarization, Windows/Linux Studio, mobile, provider-backed LLM assist, broad converter frontend, native backend lowering, proof, empirics, and promo human review. This keeps the status visible during long PR trains without making the landing page read like an internal caveat ledger or claiming full productization completion. | `python3 scripts/test_garnet_mit_readiness_status.py`; `python3 scripts/garnet_mit_readiness_status.py --format markdown`; `docs/index.html`; `docs/status.html` |
| Converter/platform strategy | The converter menu, public site, and platform handoffs preserve best-fit imports, bad direct-conversion fits, native boundaries, and future backend lowering without overclaiming support | Phase 6AX active: `F_Project_Management/GARNET_CONVERTER_AND_PLATFORM_STRATEGY.md` records the two-way architecture: import high-level agent/product logic into Garnet, keep precision-native code behind FFI/native modules with CapCaps, memory declarations, lineage, and sandbox policy, and lower Garnet out to Wasm/LLVM-style targets only after backend evidence lands. `F_Project_Management/GARNET_WINDOWS_LINUX_STUDIO_HANDOFF_2026_05_16.md` splits Codex Desktop Windows and Claude Code Windows lanes, while `F_Project_Management/GARNET_APPLE_DISTRIBUTION_WALKTHROUGH_2026_05_16.md` keeps Apple Developer ID and notarization as account-owner gated. Current evidence passes `106/106` with `skipped=0` in `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-124251`, web/PWA smoke passes in `/Users/idc2.0/Desktop/dogfood/web-pwa-readiness-20260516-124308`, and Swift Studio tests pass `26/26`. | `python3 scripts/test_garnet_converter_status.py`; `python3 scripts/test_garnet_adoption_surface_status.py`; `python3 scripts/test_garnet_converter_llm_feasibility.py`; `python3 scripts/test_garnet_converter_assist_plan.py`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench`; `swift test --package-path apps/garnet-studio-macos` |
| MIT readiness objective status | Agents and reviewers can distinguish the complete tracked implementation-plan ledger from broader MIT/productization completion | Objective-accounting gate active: `scripts/garnet_mit_readiness_status.py` reports the tracked implementation plan as verified while the broader goal remains active-partial. It keeps Developer ID notarization blocked, mobile distribution planned, promo video as composition-ready evidence in clean source checkouts, rendered-artifact evidence when local MP4/WebM outputs exist, and automated visual-QA evidence when the local QA bundle exists, LLM assist active-partial through the local context pack but not provider-backed conversion, broad converter frontends planned, and proof/empirics active-partial until each lane has its own evidence. The local objective score is 57.3% when Desktop promo render plus automated visual-QA evidence is present, reflecting local MP4/WebM and QA artifacts without treating them as website-ready. The public site and agentic matrix probe this reporter so `87/87` cannot become a false full-completion claim. | `python3 scripts/test_garnet_mit_readiness_status.py`; `python3 scripts/garnet_mit_readiness_status.py`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict` |
| Mac-side continuation accounting | Future goal runs can distinguish Mac-actionable repo/productization work from Apple account-holder and Windows/Linux target-platform gates | Phase 6AY active: `scripts/garnet_mac_side_continuation_status.py` imports the current MIT/productization objective percentage and lists reusable dogfood tooling, unsigned/local Garnet Studio quality, website/status/presentation cleanup, converter advisory quality, and proof/benchmark evidence as Mac-actionable lanes. It keeps Apple Developer ID notarization `blocked-external` on identity verification, Developer ID certificate, and notary profile prerequisites; keeps Windows/Linux Studio `handoff-only` until target-platform runtime proof exists; and records provider-backed LLM conversion plus native backend lowering as unimplemented. The agentic matrix includes a `Mac-side continuation accounting` probe so local continuation plans cannot overclaim notarized distribution, Windows/Linux completion, provider-backed LLM conversion, or native backend readiness. | `python3 scripts/test_garnet_mac_side_continuation_status.py`; `python3 scripts/garnet_mac_side_continuation_status.py`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench` |
| MIT promo public-site objective accounting | The broader objective can distinguish public-site embedding from final human/aesthetic acceptance | Phase 6AM active: when the Desktop render, visual-QA, website-export, and site-sync bundles are present, `scripts/garnet_mit_readiness_status.py` reports the promo lane as `public-site-embedded` at 95.0% and the broader MIT/productization objective as 58.6%. The remaining promo blocker is human/aesthetic acceptance review, not media rendering, visual QA, package export, or repo/site sync. | `python3 scripts/test_garnet_mit_readiness_status.py`; `python3 scripts/garnet_mit_readiness_status.py`; `/Users/idc2.0/Desktop/dogfood/garnet-promo-video-site-sync/MANIFEST.sha256` |
| Adoption surface truth | Repo and site copy can advertise compelling Garnet hooks without drifting from verified state | Process/product gate active: `scripts/garnet_adoption_surface_status.py` aggregates converter status, MIT readiness lanes, verified use cases, repo/site copy contracts, and open productization gates. It keeps Rust/Ruby/Python/Go as the only active converter lanes, planned languages as planned, provider-backed LLM conversion inactive, and use-case hooks tied to source evidence. Phase 6AC updates the macOS workbench hook and public site so Codex Run, `dist/Garnet Studio.app`, and Studio `Assist Plan` are visible without claiming notarization, broad planned-language conversion, or provider-backed LLM conversion. The agentic matrix probes this reporter so public adoption copy cannot silently overclaim. | `python3 scripts/test_garnet_adoption_surface_status.py`; `python3 scripts/garnet_adoption_surface_status.py --format json`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict`; `scripts/smoke_garnet_web_pwa.sh --copy-to-desktop --strict`; Browser smoke of `http://127.0.0.1:8765/index.html#studio` |
| Garnet Studio app distribution | macOS users can open a local app and run real Garnet workflows without starting in Terminal | App-1/App-4/App-11/App-17/App-18/Phase 6Z/Phase 6AA/Phase 6AB/Phase 6AM/Phase 6AO/Phase 6AP/Phase 6AQ/Phase 6AR/Phase 6AS/Phase 6AW active: SwiftPM SwiftUI workbench exposes CLI health, first-run onboarding, examples, converter, converter assist planning, converter advisory-bundle packaging, advisory review gating, advisory handoff packet packaging, release status, source editor, and console; package script builds `Garnet Studio.app` plus `GarnetStudio.dmg` with a bundled CLI, matrix script, status reporters, the converter assist-plan reporter, converter LLM feasibility reporter, converter advisory-bundle reporter, converter advisory-review gate, converter advisory-handoff packet builder, promo render/visual-QA/website-export/site-sync harnesses, current-truth context docs, docs/PWA assets including synced promo media, examples, packaged-app matrix smoke, packaged PWA offline-handler smoke, DMG mount/copy install smoke, manifest-verified DMG smoke evidence copied to Desktop dogfood storage, and a credential-gated Developer ID hardened-runtime signing path when `APPLE_DEV_ID_APP` is configured. Phase 6AB adds a source-checkout Run-button path: `script/build_and_run.sh` builds the SwiftPM app, stages `dist/Garnet Studio.app`, launches it as a real app bundle, and `.codex/environments/environment.toml` wires Codex's `Run` action to that script. The Converter panel keeps active deterministic conversion separate from planned-language assist planning: `Convert` is limited to Rust/Ruby/Python/Go, while `Assist Plan`, `Advisory Bundle`, `Advisory Review`, and `Advisory Handoff` can produce deterministic migration, review, and source-free handoff evidence for planned languages including C and Perl without claiming provider-backed LLM conversion. Packaged matrix runs set `PYTHONDONTWRITEBYTECODE=1` so status reporters do not create `__pycache__` entries inside the signed resource envelope, and they record source-workspace-only cargo probes as explicit packaged-resource skips when `Contents/Resources/Cargo.toml` is absent. The current local machine has no valid codesigning identity configured, so local evidence remains ad-hoc; notarization, clean-machine Gatekeeper, TestFlight/App Store, iOS, Android, real-browser offline PWA validation, and final promo human/aesthetic acceptance remain separate gates. | `python3 scripts/test_garnet_studio_run_button.py`; `./script/build_and_run.sh --verify`; `swift test --package-path apps/garnet-studio-macos`; `swift run --package-path apps/garnet-studio-macos GarnetStudio --self-test`; `swift run --package-path apps/garnet-studio-macos GarnetStudio --smoke-test`; `./scripts/package_garnet_studio_macos.sh`; `scripts/smoke_garnet_studio_dmg.sh --copy-to-desktop target/macos/GarnetStudio.dmg`; `security find-identity -p codesigning -v`; Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-105515`; `target/macos/GarnetStudio.dmg` SHA-256 `3eaee71e813a67c299411ba981eb6588a299e7ea8e7698317c6d18911423dee8` |
| Agentic dogfood stress matrix | Agents can use Garnet across orchestration, agent toolbelt examples, diagnostics, adversarial input boundaries, migration, converter assist planning, converter LLM feasibility, repo/site adoption truth, promo video readiness, safe-mode, release, signed-release provenance, macOS notarization readiness, docs, app, web/PWA productization, and memory-analysis surfaces without hidden advertised-example drift | Agentic matrix active: the first run exposed six executable gaps, which were fixed or locked with regression tests; the current source-checkout matrix includes converter adoption-status, converter assist-context and provider-neutral prompt probes, converter assist-planning TypeScript/JavaScript/Swift/Java/C/C++/C#/Perl probes, converter LLM feasibility probes, promo video readiness probes, repo/site adoption-surface truth, all three canonical project templates, formatter repair behavior, five runnable agent toolbelt examples, parse-time memory declaration analysis for the advertised log analyzer, a browser-backed PWA offline probe, signed release provenance probes, macOS notarization-readiness probes, an adversarial-boundary domain, and a Garnet Studio SwiftPM smoke that prefers the matrix-built CLI. CLI, web-api, and agent-orchestrator templates now each scaffold, run, and test under matrix control; developer experience covers doc extraction, formatter check, and formatter repair for dirty agent source; agent toolbelt coverage proves triage routing, capability budgeting, memory recall, release evidence gating, and repair planning as current-runtime examples; converter assist planning covers planned-language current truth, migration-risk inventory, and manifest verification without enabling broad conversion; converter LLM feasibility covers advisory feasibility, planned-language coverage, blocker inventory, and manifested JSON/Markdown output without activating provider-backed conversion; promo video readiness covers current truth, source-lock evidence, composition-source evidence, render-harness evidence, visual-QA-harness evidence, render/QA/website gates, and manifested JSON/Markdown output without claiming a website-ready video; adversarial-boundary coverage rejects parser depth bombs, missing `main` capability declarations, and unsafe legacy `var` declarations in `@safe` code; agent memory/analysis covers parse-time semantic/episodic/procedural memory declaration surfacing plus check/run of the advertised log analyzer; web/PWA productization covers offline-handler, local readiness, and Chrome DevTools offline navigation; signed-release provenance covers key generation, signed deterministic manifests, signature-required unsigned-manifest rejection, and signed manifest tamper rejection without preserving generated private keys in Desktop evidence bundles; macOS notarization readiness covers status reporting for preflight bundles, credential-value redaction, and missing-bundle failure behavior; repo/site adoption coverage keeps active converter lanes, planned language lanes, LLM-assist boundaries, verified use cases, and open productization gates evidence-backed; macOS app workbench covers self-test, CLI-backed workbench smoke, and XCTest. Phase 6W preserved a useful failed strict run at `58/60` before correcting stdout/stderr expectations for checker diagnostics, then passed `60/60` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-034939`. Phase 6Y passes `63/63` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-042402`; Phase 6AC reconfirmed `63/63` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-052047`; Phase 6AD reconfirmed `63/63` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-053355`; Phase 6AE expanded the converter intelligent-assist domain to four probes by checking the prompt-pack contract and passed `64/64` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-054543`; Phase 6AF expands converter assist planning to seven probes and passes `68/68` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-055816`. Phase 6AG adds the three-probe promo video readiness domain and passes `71/71` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-061302`; Phase 6AH adds source-lock evidence as a fourth promo-video probe and passes `72/72` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-063353`; Phase 6AI adds composition-source evidence as a fifth promo-video probe and passes `73/73` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-065332`; Phase 6AJ adds render-harness evidence as a sixth promo-video probe and passes `74/74` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-071841`; Phase 6AK adds visual-QA-harness evidence as a seventh promo-video probe. Phase 6AO adds C/C#/Perl planned-language assist probes and a four-probe converter LLM feasibility domain, passing `84/84` with `skipped=0` in Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-084611`. App-12 keeps first-class domain coverage adequacy separate from pass/fail readiness: all current source-checkout domains are adequate at or above `3/3`. App-6/App-10 promotes the headless-safe subset into macOS CI with uploaded evidence artifacts and fresh-builds the checkout CLI by default so stale local binaries cannot mask current-source truth; SwiftUI app probes stay covered by local/Desktop/package/DMG gates because they hang under headless SwiftPM CI. This does not claim production ARC, native backend, proof, empirical validation, notarized app distribution, cross-browser PWA certification, mobile completion, broad converter coverage, implemented LLM conversion, or a website-ready promo video. | `scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict`; `.github/workflows/agentic-dogfood-matrix.yml`; `python3 scripts/test_run_agentic_dogfood_matrix.py`; `python3 scripts/test_garnet_promo_video_status.py`; `python3 scripts/test_render_garnet_promo_video.py`; `python3 scripts/test_qa_garnet_promo_video.py`; `python3 scripts/test_garnet_converter_status.py`; `python3 scripts/test_garnet_converter_assist_plan.py`; `python3 scripts/test_garnet_converter_llm_feasibility.py`; `python3 scripts/test_garnet_adoption_surface_status.py`; `cargo test -p garnet-cli --test examples`; `cargo test -p garnet-cli --test dogfood_readiness_examples`; `shasum -a 256 -c MANIFEST.sha256` |
| Garnet Studio agentic matrix surface | macOS users can trigger the agentic dogfood matrix from the app instead of needing to remember the script path | App-2 active: Garnet Studio exposes an `Agentic Tests` section, locates the source-tree matrix via `GARNET_REPO_ROOT` or ancestor search, runs the same strict Desktop-bundle harness through `--agentic-matrix-test`, and prefers the checkout `target/debug/garnet` so stale PATH installs cannot mask current-source truth. App-3 covers packaged offline matrix assets; notarized distribution remains deferred. | `swift test --package-path apps/garnet-studio-macos`; `swift run --package-path apps/garnet-studio-macos GarnetStudio --self-test`; `swift run --package-path apps/garnet-studio-macos GarnetStudio --smoke-test`; `swift run --package-path apps/garnet-studio-macos GarnetStudio --agentic-matrix-test`; Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260515-210445` |
| Garnet Studio bundled agentic matrix | A packaged `.app` can run the agentic dogfood matrix with bundled resources instead of depending on a source checkout | App-3/App-11/Phase 6Z/Phase 6AG/Phase 6AH/Phase 6AI/Phase 6AJ/Phase 6AK/Phase 6AM/Phase 6AO/Phase 6AP/Phase 6AQ/Phase 6AS/Phase 6AW active: the app now prefers a bundled `Contents/Resources/scripts/run_agentic_dogfood_matrix.py`, passes the bundled CLI and app executable to the matrix, replaces source-tree SwiftPM app probes with packaged self/smoke probes, stages examples, docs/PWA resources, status reporters, the converter assist-plan reporter, converter LLM feasibility reporter, converter advisory-bundle reporter, converter advisory-review gate, converter advisory-handoff packet builder, the promo video readiness reporter, promo source-lock assets/surfaces, `docs/promo/` composition-source docs, synced public-site promo assets, and current-truth context documents, and verifies `--agentic-matrix-test` before DMG creation and again after mounted-DMG copy install. The runner disables Python bytecode writes so matrix/status imports do not invalidate the signed app resource envelope. Phase 6X preserved the failed packaged bundle `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-040023` (`57/60`) where the app tried to run source-workspace signed-cache cargo probes without `Cargo.toml`; the fixed packaged matrix records those three probes as `skipped` with per-probe logs while source-checkout runs continue to execute them. Phase 6Z closes the post-6Y resource dependency by staging `garnet_converter_assist_plan.py` into packaged resources before running packaged converter assist-planning probes; Phase 6AG stages `garnet_promo_video_status.py` so the promo readiness matrix probes work from packaged resources; Phase 6AH stages the canonical promo logo assets and Garnet Studio source surface so the source-lock probe also works from packaged resources; Phase 6AI relies on the full docs copy so `docs/promo/DESIGN.md` and `docs/promo/composition.html` are bundled for the composition-source probe; Phase 6AJ stages `scripts/render_garnet_promo_video.mjs` as an executable packaged render harness; Phase 6AK stages `scripts/qa_garnet_promo_video.mjs` as an executable packaged visual-QA harness; Phase 6AM stages `scripts/sync_garnet_promo_video_site.mjs` as an executable packaged site-sync harness; Phase 6AO stages `scripts/garnet_converter_llm_feasibility.py` for packaged converter-feasibility probes; Phase 6AP stages `scripts/garnet_converter_advisory_bundle.py` for packaged advisory-bundle probes; Phase 6AS stages `scripts/garnet_converter_advisory_review.py` for packaged advisory-review probes; Phase 6AV stages `scripts/garnet_converter_advisory_handoff.py` for packaged advisory-handoff probes; Phase 6AW exposes that packaged advisory-handoff script as an app action after default bundle creation and review. Current source-checkout evidence is `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-105145`; current copied-DMG evidence is `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-105515` for DMG SHA-256 `3eaee71e813a67c299411ba981eb6588a299e7ea8e7698317c6d18911423dee8`. Notarization, mobile/web distribution, and final promo human review remain separate gates. | `python3 scripts/run_agentic_dogfood_matrix.py --copy-to-desktop --strict --skip-app-workbench`; `swift test --package-path apps/garnet-studio-macos`; `/usr/bin/env PYTHONDONTWRITEBYTECODE=1 python3 "target/macos/Garnet Studio.app/Contents/Resources/scripts/run_agentic_dogfood_matrix.py" --garnet-bin "target/macos/Garnet Studio.app/Contents/Resources/garnet" --app-executable "target/macos/Garnet Studio.app/Contents/MacOS/GarnetStudio" --strict`; `./scripts/package_garnet_studio_macos.sh`; Desktop bundles `/Users/idc2.0/Desktop/dogfood/garnet-agentic-dogfood-20260516-105145` and `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-105515`; `shasum -a 256 -c MANIFEST.sha256` |
| Garnet Studio DMG install smoke | The `.dmg` can be mounted, copied to a temporary Applications-style install location, and run from the copied app bundle | App-4/App-11/App-17/Phase 6Z/Phase 6AH/Phase 6AI/Phase 6AJ/Phase 6AK/Phase 6AM/Phase 6AO/Phase 6AP active: `scripts/smoke_garnet_studio_dmg.sh` mounts the DMG read-only, copies `Garnet Studio.app`, verifies the copied app signature when `codesign` is available, checks executable bundled assets including `garnet_converter_assist_plan.py`, `garnet_converter_llm_feasibility.py`, `garnet_converter_advisory_bundle.py`, `garnet_promo_video_status.py`, `render_garnet_promo_video.mjs`, `qa_garnet_promo_video.mjs`, `export_garnet_promo_video_site.mjs`, and `sync_garnet_promo_video_site.mjs` plus docs/PWA/spec/dogfood context resources, promo source-lock inputs, `docs/promo/` composition-source files, and synced promo media, runs self-test, bundled CLI version smoke, workbench smoke, packaged PWA offline-handler smoke, and copied-app agentic matrix smoke, then writes `dmg-smoke-report.md`, `dmg-smoke-data.env`, per-command logs, packaged PWA JSON, and a verified manifest. This is still local ad-hoc signing evidence, not notarized clean-machine Gatekeeper evidence. | `./scripts/package_garnet_studio_macos.sh`; `scripts/smoke_garnet_studio_dmg.sh --copy-to-desktop target/macos/GarnetStudio.dmg`; Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-studio-dmg-smoke-20260516-090543`; `target/macos/GarnetStudio.dmg` SHA-256 `72af0dc3155fb9c7897167645b10ed4f3caca0b7680bd15a40826cc06d8cc720`; `shasum -a 256 -c MANIFEST.sha256` |
| Garnet Studio notarization preflight | Distribution signing/notarization claims are blocked unless the local app/DMG has Developer ID, hardened runtime, notarytool/stapler, and credential prerequisites | App-5/App-19 active: `scripts/preflight_garnet_studio_notarization.sh` inspects the app/DMG without submitting to Apple, writes a manifest-verified evidence bundle, and currently reports 6 blockers plus 2 warnings: ad-hoc signature, missing TeamIdentifier, missing hardened runtime, no Developer ID Application identity, missing `APPLE_DEV_ID_APP`, missing `APPLE_NOTARY_PROFILE`, Gatekeeper rejection, and no stapled DMG ticket. `scripts/garnet_studio_notarization_status.py` summarizes that bundle as JSON/Markdown for agents, PRs, and site truth while redacting credential values and explicitly avoiding notarization claims. | `scripts/preflight_garnet_studio_notarization.sh --copy-to-desktop`; `scripts/preflight_garnet_studio_notarization.sh --strict` exits 1 while blockers remain; `python3 scripts/garnet_studio_notarization_status.py --bundle <bundle>`; `python3 scripts/test_garnet_studio_notarization_status.py`; Desktop bundle `/Users/idc2.0/Desktop/dogfood/garnet-studio-notarization-preflight-20260516-021713`; `shasum -a 256 -c MANIFEST.sha256` |
| Garnet web/PWA readiness | Website visitors can install a seed Garnet docs/workbench shell and retain the core docs offline | App-7/App-9/App-14/Phase 6AC/Phase 6AD/Phase 6BA active: `docs/index.html` links a web app manifest and registers `service-worker.js`; the manifest ships 192/512 PNG icons, standalone display metadata, and scoped start URL; the service worker caches the docs shell, installer script, research/spec pages, manifest, and icons. The site now includes a Studio workbench section for Codex Run, `dist/Garnet Studio.app`, `Assist Plan`, and `Continuation Pulse` while keeping distribution and converter caveats explicit; the Pages smoke now guards that same Studio copy. App-9 adds `scripts/smoke_garnet_web_pwa_offline.mjs`, a dependency-free Node harness that executes the actual service worker in a mocked Cache API/fetch environment and proves install/activate registration, obsolete-cache cleanup, offline navigation fallback to cached `index.html`, cached static-asset fetches without network, and successful GET write-through caching. App-14 adds `scripts/smoke_garnet_web_pwa_browser.mjs`, a dependency-free Chrome DevTools Protocol smoke that launches local headless Chrome, waits for `navigator.serviceWorker.ready`, forces browser offline mode, proves offline navigation returns the Garnet shell, and fetches the manifest from the service-worker cache. This is a static docs PWA seed plus local browser/live Pages gate, not a full browser IDE, mobile app, App Store/TestFlight build, signed native distribution, or cross-browser certification. | `scripts/smoke_garnet_web_pwa.sh --copy-to-desktop --strict`; `scripts/smoke_garnet_web_pwa_offline.mjs --docs-dir docs --output target/service-worker-offline-check-phase6ad.json`; `scripts/smoke_garnet_web_pwa_browser.mjs --docs-dir docs --output target/web-pwa-browser-offline-check.json`; `python3 scripts/test_smoke_garnet_pages_pwa.py`; `scripts/smoke_garnet_pages_pwa.sh --copy-to-desktop --strict`; `.github/workflows/web-pwa-readiness.yml`; Desktop bundles `/Users/idc2.0/Desktop/dogfood/web-pwa-readiness-20260516-165857` and `/Users/idc2.0/Desktop/dogfood/pages-pwa-readiness-20260516-165857`; `shasum -a 256 -c MANIFEST.sha256` |
| Garnet live Pages PWA smoke | The public `garnet-lang.org` deployment serves the PWA shell and current Studio adoption hook after GitHub Pages publication | App-8/Phase 6AD/Phase 6BA active: `scripts/smoke_garnet_pages_pwa.sh` verifies the landing page, manifest, service worker, icon content types, manifest installability fields, service-worker offline asset list, response metadata, and the current Studio adoption copy for `Garnet Studio workbench`, Codex Run, `dist/Garnet Studio.app`, `Assist Plan`, and `Continuation Pulse` using Python stdlib HTTP requests so shell command lookup cannot be broken by zsh reserved/tied variables such as `path`. The web/PWA CI now runs `python3 scripts/test_smoke_garnet_pages_pwa.py` against a local fixture that removes those Studio phrases and must fail under `--strict`, so PR checks cover the smoke contract without depending on live deployment timing. This is a live/local deployment evidence path plus a PR-safe regression guard, not a substitute for richer offline browser-mode tests or notarized native distribution. | `python3 scripts/test_smoke_garnet_pages_pwa.py`; `scripts/smoke_garnet_pages_pwa.sh --base-url http://127.0.0.1:8876 --copy-to-desktop --strict`; `.github/workflows/web-pwa-readiness.yml`; Desktop bundle `/Users/idc2.0/Desktop/dogfood/pages-pwa-readiness-20260516-165857`; `shasum -a 256 -c MANIFEST.sha256` |

## Current Phase 1 Acceptance

- `cargo test -p garnet-parser --test parse_v1_parser_parity`
- `cargo test -p garnet-cli --test conformance_skeleton`
- `cargo test -p garnet-cli --test conformance_phase_gates`
- `cargo test -p garnet-cli --test dogfood_readiness_examples`
- `cargo test -p garnet-cli --test examples`
- canonical MVP parse/check/run loop
- `cargo audit`
- `cargo deny --all-features check`
- source trust-boundary scan for command execution, DB queries, filesystem/network authority, unsafe/eval/exec, secrets, and release-integrity surfaces
