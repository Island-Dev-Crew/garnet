## Summary

Replaces the site's June 2026 front door (three pillars, a numbers wall stamped 2026-06-07, a 390 KB inline poster) with the evidence front door from the September release pack, adapted to what the repository can prove at main `fbd64bc5`:

- An **evidence strip** whose every value links to the source that recomputes it: main commit, the signed v0.8.1 release (SBOM + GPG-signed sums), ruleset 18936562 with `bypass_actors []`, 31 required contexts, WV-6 disclosed as `partial` (U-58), one human gate.
- The wordmark, the one-line thesis with **Garnet lang** on first mention, and a tagline scoped to the registered capability surface.
- A **door field** naming every public surface; one door locked on purpose (Shelf — opens with the first sealed packages). The Playground door is open because a committed WebAssembly package runs check/run/diff-caps in the browser today. The Start door carries the MSRV sentence ("Rust 1.95+") the msrv gate requires; the release version, primitive count and workspace-test count carry xtask `<!-- truth: -->` markers (`truth --check` ok).
- An **unedited capture** of garnet 0.8.1 built at `fbd64bc5` on 2026-09-02: `garnet check` rejecting an undeclared `net::tcp_connect` (exit 1), `diff-caps --machine` reporting `authority-expanded` (exit 1), and `garnet seal` emitting an in-toto predicate marked UNSIGNED without cosign. Only long JSON lines are elided with …
- The five acts by which a change is accepted here.
- System font stacks only: the page makes zero external requests. Service-worker cache name v2→v3 so installed clients refresh.

Cross-family review by the Codex seat (read-only, detached worktree at each tip): v1 REJECT (two copy findings) → v2 REJECT (meta wording, MSRV marker, truth markers, verbatim elisions, contrast, reduced-motion) → v3 CONFIRM → the first CI firing caught two dropped gate surfaces (positioning themes; promo embed tokens) → v4 REJECT (fresh-directory capture condition; promo caption overclaim, registered U-90) → v5 CONFIRM with no findings on `b36f613e`; all lineages are recorded verbatim in `F_Project_Management/W_TRUST/FRONT_DOOR_REVIEW_2026-09-02.md` (verbatim transport).

## Dogfood Readiness

### Current truth

- Base: main `fbd64bc514b573a6735b3525f4f3172be14a27d0` (#541).
- Candidate content head: `b36f613ec2dc11f8afb0ea97bf06b6a006b97ed7` (v5); changed paths: `docs/index.html`, `docs/service-worker.js` (neither is a trust-kernel path; the rolling gate reports `ok: true`, `touched_paths []`).
- Every strip value was recomputed on 2026-09-02: ruleset via `gh api repos/Island-Dev-Crew/garnet/rulesets/18936562` (active, `bypass_actors []`, 31 contexts); release assets via `gh release view v0.8.1`; WV-6 via `scripts/garnet_wv_acceptance_status.py --wv WV-6` (partial).

### Local verification

- [x] Every `href` resolves: relative targets exist under `docs/`; every https target answers 200.
- [x] HTML parses with no mismatched tags; retired-vocabulary grep: zero.
- [x] The capture reproduces byte-for-byte from `target/release/garnet` built at the base commit on rustc 1.95 (check exit 1; diff-caps --machine exit 1; seal predicate lines identical).
- [x] Rendered at 1440 / 768 / 320 px (DOM metrics in the Browser pane): scrollWidth 1425 / 753 / 320, doors per row 5 / 4 / 2, zero external loads, zero web fonts; the capture scrolls inside its own container.
- [x] `cargo run -p xtask -- truth --check --skip-tests` ok (4 stamped surfaces); `scripts/garnet_msrv_status.py --gate` ok; `scripts/garnet_positioning_status.py --gate` ok; `scripts/garnet_promo_video_status.py` reports `source_surfaces_locked: true`, `public_site_embed_present: true`.

### Remote verification

- [x] Fresh PR checks are required to settle before handoff; no CI conclusion is claimed in advance. Pages deploys `main:/docs` on merge; the live page is re-read after deploy.

### Evidence bundle

- [x] Capture sources and outputs: `hello.garnet`, `declared.garnet`, `exfil.garnet` as shown on the page; the cross-family record named above.

### Deferred / out of scope

- [x] The og:image still carries the June social card; a new card rides the GitHub front-door lane. The union page and the tag are separate R1 items.
- [x] `docs/why.html` (trust-kernel) is untouched.
