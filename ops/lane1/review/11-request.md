# Lane 1 · Phase 0 — Review Request 11 (Slice 5 reconciled regeneration + final freeze)

- Date: 2026-07-28
- Actual implementer: **Codex (OpenAI GPT-5-based agent)** on **Hugh's
  MacBook Pro** (`Mac17,8`, arm64, macOS 26.5 / Darwin 25.5.0).
- Identity transition: earlier artifacts name Claude Code Opus 5 and Claude
  Fable 5 seats; this Slice 5 execution is Codex, not Claude. Reviewer of
  record: Codex GPT-5.6 Sol on a different machine. Cross-family separation
  is **not** claimed for this slice.
- Ceremony seat: Claude chat via Jon. Merge authority: Jon
  (`IslandDevCrew`) only. Review carrier: `IDC-Trust-Review` only. The
  implementer is neither.
- Wake tip: `8cfa5fdfa026a3d8ae718027980f494e527c8b73`
- Part A content head: `599f2a7da1c858951148dd7dd256d6c5b76f67a5`
  (tree `f8acebc286c920e49f04fe707035757876ca3c68`)
- Rebind head: `48295e5281b270384f07fae9e414d110f275afab`
  (tree `32804fa1c39ecf078fd9961cce8fe3c6e096e10f`)
- Detailed evidence: `ops/lane1/evidence/98-slice5-reconciled-regeneration.md`

## Requested verdict

Review the exact linear range `8cfa5fd..48295e5` and determine whether Slice 5
may proceed to native-Windows WV-6 acceptance at the frozen product pair.

## Part A — sanctioned regeneration

Exact producer commands:

```text
python3 scripts/garnet_launch_readiness_status.py --format json > ops/lane0/evidence/08-launch-readiness.json
python3 scripts/garnet_mit_readiness_status.py --committed-only --format json > ops/lane0/evidence/09-mit-readiness.json
python3 -I scripts/garnet_lane0_closeout_status.py --write-denominators
python3 scripts/garnet_launch_readiness_status.py --format markdown > F_Project_Management/LAUNCH/LAUNCH_READINESS.md
python3 /private/tmp/garnet-l1-s5-state-producer.py /private/tmp/garnet-l1-s5-20260728
node ops/mission/render-sotu.mjs
python3 -I scripts/garnet_lane0_closeout_status.py --seal --run-id lane0-20260716-3124ba5
```

The committed outputs prove:

- `08.source == "scripts/garnet_launch_readiness_status.py"` exactly.
- `09.source == "committed-truth"`; its regenerated bytes are unchanged.
- launch remains `HOLD`.
- denominators are exactly `19/19`, `65.2/70`, `3/6`, `3/8`.
- mission state records slice-4 WV-6 acceptance, U-31 cured/approved plus its
  native-Windows proof, U-17 open, U-36 untouched, and the actual Codex/Mac
  implementer identity.
- the SOTU was rendered from state and never hand-edited.
- manifest and ARCHIPELAGO ledger were resealed by the existing closeout
  producer; Lane 0 closeout is PASS again.

Part A diffstat:

```text
F_Project_Management/LAUNCH/LAUNCH_READINESS.md |   5 --
ops/lane0/evidence/08-launch-readiness.json     |   8 +-
ops/lane0/evidence/10-denominators.json         |   2 +-
ops/lane0/evidence/MANIFEST.sha256              |   4 +-
ops/lane0/ledger.jsonl                          |  70 ++++++++--------
ops/mission/state-of-the-union.html             |  27 ++++--
ops/mission/state.json                          | 105 +++++++++++++++++++++---
7 files changed, 152 insertions(+), 69 deletions(-)
```

## F3 flip and differential

- Before: launch reporter suite 41 tests, exactly one failure:
  `test_tracked_ledger_matches_renderer_byte_for_byte`.
- After: the same suite is 41/41.
- Full isolated Python battery before: 1,130 tests, five failures, zero errors.
- After: 1,130 tests, four failures, zero errors.
- The only removed failure is F3. The successor-only failure set is empty.
- The four remaining identifiers are unchanged: adoption-surface, novel
  compositions, release assets, and expected WV-6 exact-candidate partial.

The initial Homebrew `python3 -I` battery was invalid because isolation hid
user-site PyYAML. This degradation is recorded rather than skipped; both
authoritative differential runs used the same isolated venv with exact
`PyYAML==6.0.3` and `jsonschema==4.26.0`.

## Part B — frozen product pair and rebind

Repository function result at Part A head:

```text
head    599f2a7da1c858951148dd7dd256d6c5b76f67a5
tree    f8acebc286c920e49f04fe707035757876ca3c68
digest  ea38d3547eafa7f56141454df50eaf8084dbc66ee9573ebdd67623df8be97bbe
count   1544
```

The rebind changes only the candidate mirrors in the excluded Shelf reporter
and `proofs/**`. The four semantic values are head, tree, digest, and count;
the count was already `1544`, so only three lines per file required byte
replacement. Historical `1e669217… / 1527` and every `reviewedTree*` historical
field remain byte-identical.

Recomputation at `48295e5` remains exactly `ea38d354… / 1544`. The Shelf gate
returns `ok:true`, `findings:[]`, five checks true. The WV-6 acceptance
reporter stays correctly `partial` because the NUC has not yet rebound the
native-Windows manifest to this reviewed candidate.

## Post-freeze invariant

Every commit after the Part A freeze is digest-inert:

- rebind: exact excluded reporter path + `proofs/**`;
- request/evidence/heartbeat: `ops/lane1/**`.

No included product path changes after `599f2a7`. The fork branch was first
pushed at rebind head `48295e5`; request 11 and its heartbeat are the only
later changes and remain excluded from the product digest.

## Requested checks

1. Recompute `599f2a7` head/tree and `ea38d354… / 1544` through the repository
   provenance function.
2. Verify Part A producer-owned diffs, exact POSIX `08.source`, and honest
   `3/6`, `3/8`, `HOLD`.
3. Reproduce the F3-only failure-set reduction.
4. Verify the rebind touched only the authorized candidate mirrors; historical
   pair and exclusion predicate remain unchanged.
5. Verify the rebind head and every later request artifact preserve
   `ea38d354… / 1544`.
6. Name the exact head approved for the NUC. Until a verdict does so, no
   native-Windows acceptance run is authorized.

## STOP

The implementer stops after pushing request 11. No PR, record, approval,
merge, NUC contact, or acceptance emitter action is performed.
