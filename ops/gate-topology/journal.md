# Gate Topology Journal

- 2026-08-11T17:47:18Z — Fresh clone from `Island-Dev-Crew/garnet` with `origin/main` and lane base both `efd4f6bae8b3afaba74594e57944b2548142aeae`; `core.autocrlf=false`; no pull refs fetched. Boot: Hugh’s MacBook Pro, Darwin 25.6.0 arm64, OpenAI Codex GPT-5-based agent (exact version unavailable). Mission state: P7 active, U-17 OPEN, WV-6 partial, launch HOLD. Five-gate truth floor PASS.
- 2026-08-11T18:00:23Z — Corrected RED target verified: `162b96a` is one Claude Fable 5 record-only commit atop `8ae41b6`, with its delta confined to `ops/wv6-reaccept/**`. The unamended v2 trust gate reproduced both graph findings verbatim at `162b96a` and exited 1. U-35 record movement independently recomputed through the reviewer's final record: `fd96e6d9…/1606 → d7a08be5…/1607 → 49e5686c…/1608 → d0978d1c…/1609`. No script byte has changed; this is the RED-first boundary.
- 2026-08-11T18:04:52Z — Added the bounded merge-parent fixture before touching the gate implementation. Under the unamended v2 walk, the equal-snapshot case fails with both topology finding classes while the one-trust-byte mutation case stays red: 2 tests run, 1 expected failure. The verbatim failure is evidence 03.
- 2026-08-11T18:10:46Z — Slice 2 GREEN: the walk now derives the reviewed lineage, admits an outside merge parent only after exact trust snapshot equality and a separately computed SHA-256 digest over the trust bytes match `reviewed_head`, and otherwise preserves the old comparisons. The bounded fixture is 2/2 green (equal topology admitted; one-byte mutation still red); the full trust-gate suite is 112/112 green. The root procedural contract now records the deterministic merge-topology rule.
- 2026-08-11T18:12:38Z — Fixture-scope correction, preserved rather than erased: Evidence 03's RED topology reproduction is valid, but its mutation description was not — the fixture added a trust file rather than changing one byte. Evidence 03a reruns the corrected `alpha = 1`→`alpha = 2` single-byte fixture against the historical unamended module and remains RED; Evidence 04 reruns it against the amendment and is 2/2 GREEN. Evidence 03a supersedes only Evidence 03's mutation-scope sentence.
