# Lane 0 Repair 3 — Review Request 01: U-25

## Seats

- Implementer: OpenAI Codex, GPT-5-based runtime; exact version unavailable
- Implementer machine: Hughs-MacBook-Pro.local, macOS 26.5, arm64
- Independent reviewer: UNASSIGNED — must be a non-OpenAI model family on a different machine
- Review carrier: IDC-Trust-Review only
- Merge authority: Jon (IslandDevCrew) only

The implementer does not approve this packet and will not author its verdict.

## Exact boundary

- Base: `efd4f6bae8b3afaba74594e57944b2548142aeae`
- Implementation head: `be89de21684c84bb0aaae8382906864a48637d20`
- Implementation tree: `1faeed401996985d9b6d8412225ab706783c3355`
- Trust-path digest reported by the rolling gate:
  `sha256:7e3ba611451e8f20bb7fa3f4edd39801698b96e29c9a7eac0a0bb653649b0866`
- RED: `ops/lane0-repair3/evidence/05-u25-scope-red.txt`
- GREEN: `ops/lane0-repair3/evidence/06-u25-green.txt`
- Exact-tree scope: `ops/lane0-repair3/U25_SCOPE.json`

## Change under review

1. `scripts/garnet_text_byte_policy_status.py` enumerates CR-bearing text paths
   at an exact commit/tree. It excludes `proofs/**` and
   `ops/**/evidence/**`, names violations, and has no total/count contract.
2. The two `.dogfood` JSON ledgers and
   `D_Executive_and_Presentation/garnet-website.html` were mechanically
   normalized from CRLF to LF. Their consumers remain semantic, not byte-hash
   based.
3. `.gitattributes` explicitly pins those three assets plus generated Mission
   Control HTML to `text eol=lf`. The later `proofs/** -text` override remains
   effective.
4. `ops/lane2b/render-sotu.mjs` now rejects carriage returns in state, journal,
   and rendered output. The HTML was regenerated by that script, not hand-edited.
5. The six sealed S114 proof paths were not changed. Their original blob OIDs
   and SHA-256 values remain enumerated in `U25_SCOPE.json`.
6. Proposed U-45 is registered as proposed, not represented as an accepted
   governance decision.

## Required reviewer checks

1. Reproduce the four-path RED at base and empty violation list at the exact
   implementation head.
2. Confirm the checker cannot false-green by hiding non-evidence `ops/**`
   paths, binary files, lone CR, malformed refs, duplicates, or unsafe paths.
3. Confirm no field or assertion pins a repository-wide count.
4. Confirm the proof/evidence exclusions do not weaken their byte-exact
   manifest gates and all six sealed blobs are unchanged.
5. Confirm JSON parsing semantics and the presentation HTML content are
   unchanged apart from line-ending normalization.
6. Confirm the renderer fails closed on CR-bearing inputs and emits LF-only
   output.
7. Confirm implementer/reviewer identity separation and record any harness
   transition truthfully.

Verdict destination:
`ops/lane0-repair3/review/01-u25-verdict.md`
