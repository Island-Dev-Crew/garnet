# S56 Plan — playground MVP

Contract: `F_Project_Management/GARNET_v0_8_SLICE_DOGFOOD.md` → S56.
Map: reconciled plan §77, §156 — playground (adoption driver). Existing
docs/playground.html is an honest "planned, no fake editor" placeholder; respect
that stance.
Branch: `codex/s56-playground`. Base: `origin/main` @ `0b5ab98` (S55).

## Approach (static gallery, NOT a fake live editor)
Live execution needs the deferred S55 wasm build. So S56 ships a STATIC gallery:
browse real programs + their recorded `garnet run` output. No browser execution
claim.

## Deliverables
- `docs/playground.html`: upgraded to a static gallery (example selector +
  source + recorded output) reading `docs/playground/examples.json`; keeps the
  honesty banner ("static / not shipping a fake editor / WebAssembly").
- `docs/playground/examples.json`: generated by `scripts/garnet_playground_build.py`
  (3 curated examples: hello, documented_math, mvp_05_web_app; stdout recorded).
- `scripts/garnet_playground_readiness.py`: validate gallery well-formed + page
  honesty markers; `--gate` fails on malformed gallery or lost marker.
- `scripts/test_garnet_playground_readiness.py`: 4 unit tests.
- Wire test + `--gate` into ci.yml agent-contracts.

## Dogfood
- `garnet_playground_readiness.py --format md` → gallery OK (3 examples, honesty
  markers present); `--gate` exits 0.

## Honest scope (do not soften)
- Static gallery, NOT a live editor; outputs recorded, not browser-computed. Live
  execution waits on the S55 wasm build (deferred). The gate enforces the page
  still says it's not a fake editor. No new readiness lane.

## Gates
- generator (local) + reporter/gate + tests + ladder (zero Rust changed). Ledger:
  `s55 → merged(5)` advanced this branch; `s56` rides with S57.
