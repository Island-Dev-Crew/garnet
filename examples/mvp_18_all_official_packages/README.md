# S18 All Official Packages Smoke

This project is a local-registry dogfood target for S18. It exercises one
primitive from each first-wave Layer-2 package after `garnet add --registry`
vendors them into `.garnet/vendor/`.

It is not proof that the external `github.com/garnet-lang/*` repos exist.
