# @garnet-lang/cli

Status: local-registry seed for v0.7. This package provides pure command-line
shape helpers; it does not parse the host process argv yet.

Stability: `experimental`. Capability ceiling: none.
